# Part XXIX — Graphics Algorithms

Parts I–XXVIII establish the Linux graphics stack from kernel DRM internals through Wayland compositor architecture, Vulkan API layers, browser rendering engines, terminal emulators, multimedia frameworks, and display hardware. Part XXIX steps back from the stack itself to examine what runs *on* it: the algorithms and mathematical techniques that GPU programs execute once the pipeline is correctly constructed. The twenty-eight chapters here are reference catalogs, not tutorials — they collect in one place the named techniques that graphics, geometry, and compute programmers reach for repeatedly, with enough description to select the right approach and enough references to implement it correctly.

These chapters address three audiences simultaneously. **Graphics application developers** selecting a shading or geometry technique before committing to an implementation will find the trade-off analysis most useful. **Systems and driver developers** performance-budgeting a GPU workload need to know what class of algorithm is running — register pressure, memory access pattern, divergence profile — and the catalog entries surface those characteristics directly. **Browser and web platform engineers** mapping WebGPU or WebGL workloads onto Linux GPU capabilities will find the per-algorithm extension and API-support notes relevant.

The unifying theme is **GPU-resident data parallelism**: every algorithm here is either already data-parallel or is expressed in a parallel form that maps onto Vulkan compute dispatches, vertex/fragment shader invocations, or hardware fixed-function stages. Where a CPU-centric library remains the practical production path (OpenSubdiv's topology refiner, meshoptimizer's encoder, OpenCASCADE's B-rep mesher), the relevant chapter explains the GPU upload and integration pattern so the CPU pre-pass feeds a GPU-resident pipeline.

## Chapter List

**Shader Algorithm Catalog**
- [Ch 204: Shader Algorithm Catalog — Rendering Pipeline, Lighting, and Shadows](ch204-shader-algorithm-catalog.md)
- [Ch 205: Shader Algorithm Catalog — Global Illumination and Materials](ch205-shader-gi-and-materials.md)
- [Ch 206: Shader Algorithm Catalog — Ray Tracing and Procedural Content](ch206-shader-raytracing-and-procedural.md)
- [Ch 207: Shader Algorithm Catalog — Visual Effects, Post-Processing, and GPU Compute](ch207-shader-vfx-postprocess-compute.md)

**GPU Geometry Algorithms**
- [Ch 208: GPU Geometry Algorithms — Surface Representation and Mesh Processing](ch208-gpu-geometry-algorithms.md)
- [Ch 209: GPU Geometry Algorithms — Spatial Structures, Differential Geometry, and Animation](ch209-gpu-spatial-differential-animation.md)
- [Ch 210: GPU Geometry Algorithms — Physics Simulation and Volumetric Methods](ch210-gpu-physics-and-volumetric.md)
- [Ch 211: GPU Geometry Algorithms — Terrain, Ray Tracing Geometry, and Point Clouds](ch211-gpu-terrain-raytracing-pointcloud.md)
- [Ch 212: GPU Geometry Algorithms — Neural Geometry, Specialized Techniques, and GPU Primitives](ch212-gpu-neural-specialized-primitives.md)

**Specialist Algorithm Chapters**
- [Ch 220: GPU Image Processing Algorithms](ch220-gpu-image-processing-algorithms.md)
- [Ch 221: GPU Algorithm Performance and Optimization](ch221-gpu-algorithm-performance.md)
- [Ch 222: Computational Geometry Algorithms on GPU](ch222-computational-geometry-algorithms.md)
- [Ch 223: GPU Video Processing Algorithms](ch223-gpu-video-processing-algorithms.md)
- [Ch 224: 3D Shape Analysis Algorithms](ch224-3d-shape-analysis-algorithms.md)
- [Ch 225: Computational Topology Algorithms on GPU](ch225-computational-topology-algorithms.md)

**Mathematical and AI Algorithm Foundations**
- [Ch 226: GPU Linear Algebra and Sparse Solvers](ch226-gpu-linear-algebra-sparse-solvers.md)
- [Ch 227: GPU Random Number Generation and Monte Carlo Methods](ch227-gpu-rng-monte-carlo.md)
- [Ch 228: GPU Graph Algorithms](ch228-gpu-graph-algorithms.md)
- [Ch 229: GPU Machine Learning Inference Algorithms](ch229-gpu-ml-inference-algorithms.md)
- [Ch 230: GPU Signal Processing and Audio DSP](ch230-gpu-signal-processing-audio-dsp.md)
- [Ch 231: GPU Compression Algorithms](ch231-gpu-compression-algorithms.md)
- [Ch 232: GPU Generative AI and LLM Inference on Linux](ch232-gpu-generative-ai-llm-inference.md)

**3D Rendering Algorithm Chapters**
- [Ch 233: GPU Denoising Algorithms](ch233-gpu-denoising-algorithms.md)
- [Ch 234: GPU Spectral Rendering and Colorimetric Algorithms](ch234-gpu-spectral-rendering-colorimetric.md)
- [Ch 235: GPU Vector Graphics and 2D Path Rendering](ch235-gpu-vector-graphics-2d-rendering.md)

**Scene Analysis Algorithm Chapters**
- [Ch 236: GPU 3D Scene Understanding and Semantic Segmentation](ch236-gpu-scene-understanding-segmentation.md)
- [Ch 237: GPU Depth Estimation and Dense Reconstruction](ch237-gpu-depth-estimation-reconstruction.md)
- [Ch 238: GPU Object Detection and 6DoF Pose Estimation](ch238-gpu-object-detection-pose-estimation.md)

## Chapters in This Part

### Shader Algorithm Catalog (Ch 204–207)

The Shader Algorithm Catalog spans four volumes covering 108 named techniques across 13 categories. Every entry follows the same structure: what the algorithm computes (one sentence), when to use it, named variants, costs and limitations, and a primary reference URL. The catalog is intended as a decision tool at the whiteboard stage — before a line of shader code is written — and as a vocabulary reference for reading GPU-intensive codebases and papers.

**Chapter 204 — Rendering Pipeline, Lighting, and Shadows** covers the three foundational categories: rendering architecture (GPU-driven indirect, Clustered Forward+, Deferred/G-Buffer, Visibility Buffer, VRS, Shader Execution Reordering, Foveated/Multiview, Meshlet Generation), direct and area lighting (punctual/IES, LTC area lights, capsule/tube lights, volumetric shafts, caustics), and shadows and occlusion (shadow maps, CSM, omnidirectional, PCSS, ESM, shadow atlas, SSAO, bent normal/AO baking). These three categories are prerequisites for every technique in the subsequent volumes.

**Chapter 205 — Global Illumination and Materials** covers the algorithms that determine surface radiance: global illumination (GI survey, IBL, dynamic env map, lightmapping, stochastic GPU path tracing, LPV, SSGI, world-space radiance cache, spherical harmonics, spherical Gaussians), material models and BRDF (PBR Cook-Torrance, anisotropic, clear coat/sheen, glint/sparkle, iridescence/thin-film, translucency, SSS, eye rendering, NPR), and character and organic rendering (skin/hair, fur/shell, cloth, skeletal animation/skinning).

**Chapter 206 — Ray Tracing and Procedural Content** covers non-rasterisation techniques and world building: ray tracing (shader patterns, complete path tracer, ReSTIR PT, RT denoising SVGF/NRD, ray differentials, software BVH traversal), procedural content and geometry (noise, SDF sphere marching, 3D SDF voxelization, isosurface extraction, Catmull-Clark GPU, displacement via compute, phong/adaptive tessellation, mesh shader patterns), and environment and simulation (terrain/vegetation, terrain splatting, water/ocean, fluid SPH/Eulerian, GPU particles, fog, participating media, direct volume rendering).

**Chapter 207 — Visual Effects, Post-Processing, and GPU Compute** is the final Shader Catalog volume. It covers screen-space surface detail (virtual texturing, bindless, POM, MSDF font), transparency and compositing (OIT, SSR, decals, 3D Gaussian Splatting, neural rendering primitives), post-processing and image effects (TAA, DLSS/FSR/XeSS, bloom, DoF, motion blur, colour grading, 3D LUT, auto-exposure, dithering), and GPU compute primitives (wave/subgroup intrinsics, radix sort, bitonic sort, cooperative matrices, spatial hash structures, sampling). Chapter 207 contains the full References section for the Ch 204–207 series.

### GPU Geometry Algorithms (Ch 208–212)

The GPU Geometry Algorithms series spans five volumes covering 107 algorithms across 13 categories. **Chapter 208 contains the Algorithm Selection Guide** — decision tables by problem type and runtime constraint covering all 13 geometry categories — which should be read before any of the five volumes when the right category is not yet known.

**Chapter 208 — Surface Representation and Mesh Processing** covers the Algorithm Selection Guide for all 13 geometry categories, surface representation (subdivision surfaces/OpenSubdiv/Catmull-Clark/Loop, NURBS/B-Spline, metaballs/implicit surfaces, spline tessellation, 2D vector graphics on GPU, CAD curve rendering, GPU Boolean CSG, SDF blending, Poisson surface reconstruction), and mesh processing and topology (simplification/LOD, remeshing, UV seam/atlas, voxelisation, progressive meshes, compression/streaming, UV unwrapping, GPU Delaunay, smoothing/fairing, segmentation, repair/waterproofing, constrained Delaunay).

**Chapter 209 — Spatial Structures, Differential Geometry, and Animation** covers acceleration and indexing structures (GPU BVH construction/traversal, screen-space geometry, GPU-driven virtual geometry, octree/k-d tree/hash grid, precomputed visibility, occlusion queries, sparse voxel DAG, SDF baking/3D jump flooding, virtual geometry cluster LOD), differential geometry (geodesics, spectral geometry, shape morphing/functional maps, heat method, spectral mesh processing), and animation (skeletal LBS/DQS, IK solvers, blend shapes, hair/strand geometry, secondary dynamics, mesh retargeting, soft-body FEM, cage-based deformation, Laplacian mesh editing).

**Chapter 210 — Physics Simulation and Volumetric Methods** covers GPU simulation dynamics (fluid SPH/FLIP/Eulerian, deformable FEM/projective dynamics, rigid body, crowd/multi-agent, fracture/destruction, rope/cable, GPU cloth, fluid-solid coupling, granular DEM, hydraulic erosion, fluid surface extraction, CCD, Minkowski sum/motion planning) and SDF and volumetric methods (level set evolution, swept volumes, TSDF fusion/3D reconstruction, SDF collision, isosurface extraction Marching Cubes/Dual Contouring, GPU AO, volumetric ray marching, sparse narrow-band level-set).

**Chapter 211 — Terrain, Ray Tracing Geometry, and Point Clouds** covers terrain and environment geometry (procedural geometry, nav mesh/GPU pathfinding, terrain LOD/streaming, GPU particles/ribbon, ocean/wave simulation, geospatial terrain), ray tracing and optical geometry (RT geometry pipeline TLAS/BLAS, shadow geometry, IBL/env map preprocessing, GI probe geometry, geometric optics/caustics/beam tracing, SSR, GPU radiosity, path tracing geometry, atmospheric scattering, SSS geometry, OIT, polygon/frustum clipping), and point cloud processing (3D Gaussian Splatting, point splatting, structure-from-motion/MVS, 3D feature descriptors/pose estimation, ICP registration).

**Chapter 212 — Neural Geometry, Specialized Techniques, and GPU Primitives** is the final Geometry Algorithms volume. It covers neural and learned geometry (3D Gaussian Splatting, geometric deep learning, differentiable rendering, NeRF/neural implicit surfaces), specialized and cross-domain applications (scientific visualization, decal projection, procedural texture synthesis, acoustic ray tracing, VR/XR reprojection/foveation, silhouette detection, micro-polygon displacement, SDF font rendering, molecular surface), and GPU algorithm primitives (Poisson disk/blue noise sampling, GPU convex hull, GPU radix sort). Chapter 212 also contains §108 (Library Landscape — production geometry library survey), §109 (Performance Reference), and §110 (Integrations cross-reference map for Ch208–212).

### Specialist Algorithm Chapters (Ch 220–225)

Six additional chapters complete Part XXIX by covering algorithm domains that sit adjacent to or span across the Shader and Geometry catalogs but warrant dedicated treatment because of their distinct pipeline requirements, library ecosystems, or target audience.

**Chapter 220 — GPU Image Processing Algorithms** covers the algorithms that operate on 2D raster data on the GPU: separable and bilateral spatial filters, morphological operations (erosion/dilation/opening/closing), Canny edge detection and Harris corner detection, FFT-domain processing (VkFFT, Wiener deconvolution), optical flow (TV-L1, RAFT), stereo matching via SGM, neural super-resolution (ESRGAN, AMD FSR 2.x), 3D LUT colour processing, and the Linux ISP pipeline via libcamera IPA. It also covers the OpenCV GPU backends (UMat, cv::cuda::GpuMat) and the Halide Vulkan target. This chapter is the image-processing counterpart to the video-processing algorithms in Ch223, and supplies the per-frame filtering stage that feeds GStreamer and FFmpeg pipelines.

**Chapter 221 — GPU Algorithm Performance and Optimization** is a cross-cutting performance reference for all algorithms in Part XXIX. It presents the roofline model and arithmetic intensity analysis, occupancy limiters (register/shared-memory/block-size), memory coalescing and shared memory bank-conflict avoidance, wave/warp divergence handling, barrier cost and split-barrier patterns, persistent kernels, subgroup and cooperative matrix primitives, multi-queue async compute scheduling, and Linux profiling tools (AMD RGP, NVIDIA Nsight Graphics, intel_gpu_top, VK_EXT_performance_query). The chapter closes with vendor-specific guidance for AMD RDNA wave32/wave64, Intel Xe, and NVIDIA Ampere/Ada architectures.

**Chapter 222 — Computational Geometry Algorithms on GPU** bridges classical computational geometry and GPU parallelism. It covers GPU convex hull (quickhull parallel partition step), 2D polygon Boolean operations (Sutherland-Hodgman, Clipper2, Greiner-Hormann), triangulation (ear-clipping GPU, CGAL CDT, libtess2), point location via ray casting and winding-number GPU shaders, an N×M intersection test suite (ray-AABB/OBB/sphere/triangle, OBB-OBB SAT), Voronoi/Delaunay via jump flooding, approximate nearest neighbour search via FAISS GPU, GJK/EPA collision detection, geometric arrangements, 2D/3D visibility, and robust predicates (Shewchuk adaptive arithmetic).

**Chapter 223 — GPU Video Processing Algorithms** covers the codec algorithm layer that sits above the hardware fixed-function engines described in Ch165 (Vulkan Video). It explains GPU-parallelisable stages: motion estimation (SAD GPU kernel, diamond/UMH search), intra prediction (H.264/HEVC/AV1 angular modes, GPU wavefront parallelism), DCT butterfly transforms and warp-shuffle GLSL implementations, RDOQ trellis quantisation, deblocking and loop filters (H.264/HEVC/AV1 CDEF/SAO/Wiener), temporal deinterlacing (YADIF, bwdif Vulkan), FILM frame interpolation, video super-resolution (BasicVSR++), HDR/PQ/HLG tone mapping in GLSL, 360° video projection, scene-cut and content analysis, and GStreamer/FFmpeg Vulkan filter graph integration with DMA-BUF zero-copy.

**Chapter 224 — 3D Shape Analysis Algorithms** addresses the full pipeline for understanding 3D geometry beyond rendering: local descriptors (FPFH, SHOT, ROPS), global descriptors (3D Zernike, D2 distribution, spherical harmonics), spectral analysis via the Laplace-Beltrami operator (HKS, WKS, GPS), functional maps for non-rigid correspondence, symmetry detection, MCF skeleton extraction, shape segmentation (random walks, normalised cuts, PointNet++), shape completion (PDE harmonic, PCN/FoldingNet), shape retrieval via FAISS GPU index, and geometric deep learning (GCN, MeshCNN, DiffusionNet, PyTorch3D). Production library coverage includes Open3D GPU tensor backend, PCL GPU modules, CGAL Shape Detection, and libigl.

**Chapter 225 — Computational Topology Algorithms on GPU** covers topological data analysis techniques with graphics and mesh-processing applications. It presents Euler characteristic and genus computation, simplicial complex construction (Vietoris-Rips, alpha complex), persistent homology via Ripser++ GPU and CubicalRipser for voxel data, discrete Morse theory GPU vector field pairing, Reeb graphs and contour trees via GPU union-find (TTK), handle and tunnel detection via harmonic 1-forms (cuSPARSE/rocSPARSE), the Mapper algorithm with cuML DBSCAN clustering, topological scalar field simplification, and topological mesh repair (non-manifold detection, BFS orientation, Liepa hole filling). The chapter closes with topology-aware rendering applications: persistence-filtered marching cubes, topology-preserving LOD, and TTK–ParaView pipeline integration.

### Mathematical and AI Algorithm Foundations (Ch 226–232)

Seven chapters cover the mathematical substrate and AI inference layer that the rest of Part XXIX builds on. These chapters are cross-cutting: each provides foundational algorithms that multiple domain-specific chapters (geometry, image processing, video, topology) call into.

**Chapter 226 — GPU Linear Algebra and Sparse Solvers** covers the full LA stack from dense BLAS (batched GEMM, TRSM, SYMM/SYRK with register blocking) through dense factorizations (LU, QR, Cholesky, SVD, batched Jacobi SVD) and eigensolvers (LOBPCG, Lanczos/Arnoldi with thick-restart), to sparse matrix formats (CSR/ELLPACK/HYB/BSR), SpMV load-balancing strategies, SpGEMM symbolic+numeric phases, iterative solvers (CG, BiCGSTAB, GMRES), and multigrid AMG (AMGX, rocALUTION). Production library coverage includes rocBLAS, rocSOLVER, rocSPARSE, Intel oneMKL (SYCL), MAGMA, and VkFFT. Mixed-precision treatment covers FP64→FP16 iterative refinement and VK_KHR_cooperative_matrix GEMM tiles. This chapter is the implicit substrate for physics simulation (Ch210), shape analysis eigensolvers (Ch224), topological Laplacians (Ch225), and ML inference GEMMs (Ch229).

**Chapter 227 — GPU Random Number Generation and Monte Carlo Methods** covers the sampling foundation assumed by the path-tracing and stochastic GI chapters. It explains PRNG taxonomy (LCG, XORSHIFT128+, PCG, counter-based Philox/Threefry/ARS), quasi-random low-discrepancy sequences (Halton, Sobol with Joe-Kuo tables, Owen scrambling), sampling distribution transforms (Box-Muller, Ziggurat, Walker alias method), multiple importance sampling strategies (power/balance heuristic, cosine-weighted hemisphere, GGX VNDF), reservoir sampling (A-Res, A-ExpJ), the full ReSTIR DI three-pass Vulkan compute pipeline, GPU MCMC (Metropolis-Hastings, HMC leapfrog, parallel tempering), and Monte Carlo integration estimators (NEE, BDPT, environment map 2D CDF). Libraries: rocRAND, hipRAND, oneMKL PRNG.

**Chapter 228 — GPU Graph Algorithms** covers parallel graph computation from the SIMT/irregular-memory-access mismatch through production library APIs. Algorithms covered: direction-optimising BFS (Beamer top-down/bottom-up), delta-stepping SSSP, GPU Bellman-Ford, ECL-CC connected components (adaptive granularity union-find), PageRank SpMV power iteration, Borůvka MST, JP graph coloring, Luby's MIS, triangle counting (warp-per-edge set intersection), Louvain community detection, and GPU GNN inference (GraphSAGE scatter-gather, GAT sparse attention). Libraries: cuGraph (RAPIDS), Gunrock v2 operator model, SuiteSparse:GraphBLAS/LAGraph. Includes an accurate note that "hipGraph" on AMD refers to execution graphs, not analytics — RAPIDS on ROCm is the correct path for GPU graph analytics on AMD hardware.

**Chapter 229 — GPU Machine Learning Inference Algorithms** covers the inference algorithm layer used by neural super-resolution (Ch220), video SR (Ch223), and neural geometry (Ch212). It explains dense-layer im2col/Winograd Conv2D lowering, FlashAttention-2 tiled online softmax (and FlashAttention-3 for Hopper), GQA/MQA KV-cache layout, post-training quantization (GPTQ Hessian rounding, AWQ activation-aware, SmoothQuant, GGUF block layout), speculative decoding variants (Medusa, EAGLE), BN/activation fusion, MLIR-IREE graph fusion, and neural rendering inference (NeRF, 3DGS SH eval, diffusion UNet). Runtime coverage is Linux-first: ONNX Runtime MIGraphX EP, TensorRT, mlc-llm (TVM ROCm/Vulkan), llama.cpp Vulkan, Kompute, Dawn WebGPU WGSL attention shaders.

**Chapter 230 — GPU Signal Processing and Audio DSP** covers the 1D signal processing domain that extends beyond the 2D image FFT in Ch220. It treats multi-batch 1D/2D/3D FFT (rocFFT plan API, VkFFT configuration), overlap-add/overlap-save convolution including partitioned IR for real-time reverb, FIR design (windowed sinc, Parks-McClellan, polyphase SRC) and GPU multichannel compute shaders, IIR biquad with look-ahead prefix-product parallelisation, discrete wavelet transform (lifting scheme, separable 2D DWT in shared memory), WOLA/MDCT/QMF filter banks, HRTF binaural rendering and Higher-Order Ambisonics GPU decode, STFT/Mel/MFCC/CQT spectral analysis, YIN/MPM pitch detection via FFT autocorrelation, and a real-time PipeWire/JACK GPU processing pipeline with ping-pong VkBuffer and timeline semaphore audio-render synchronisation.

**Chapter 231 — GPU Compression Algorithms** covers the full compression stack relevant to GPU pipelines: BC1–BC7 block compression (mode selection, endpoint/index optimization), ASTC (variable block footprints, astc-encoder GPU backend), ETC2/EAC/PVRTC for embedded Linux targets, GPU texture compression encoders (Compressonator, bc7enc_rdo, real-time BC7 encode pipeline), Draco geometry compression (edgebreaker, parallelogram predictor), meshoptimizer encode/decode, nvcomp/hipcomp lossless (LZ4, Snappy, GDeflate), parallel rANS/tANS entropy coding, GPU JPEG/PNG encode pipelines, and framebuffer display compression (ARM AFBC, AMD DCC, Intel FBC) including DRM format modifier flags. The chapter closes with VK_EXT_image_drm_format_modifier pipeline integration for zero-copy compressed texture workflows.

**Chapter 232 — GPU Generative AI and LLM Inference on Linux** covers the two dominant generative workloads now running on Linux GPU hardware: LLM autoregressive decode and diffusion model sampling. It explains the LLM transformer compute graph (QKV GEMM, FlashAttention, FFN) for GQA/MoE architectures, PagedAttention KV-cache block management, W4A16 quantization (GPTQ/AWQ/GGUF), speculative decoding (Medusa/EAGLE), diffusion pipeline stages (VAE, UNet attention, DDIM/DPM-Solver++ schedulers), ROCm stack deployment (MIGraphX, vLLM ROCm, llama.cpp HIP), Vulkan compute inference (llama.cpp SPIR-V shaders, VK_KHR_cooperative_matrix, Kompute), and Linux deployment patterns (Ollama, LocalAI, ComfyUI ROCm, systemd service, Podman device passthrough). Performance profiling covers the roofline analysis for decode-phase bandwidth-boundedness and rocprof trace methodology.

### 3D Rendering Algorithm Chapters (Ch 233–235)

Three chapters address rendering algorithm domains that span or fill gaps between the Shader Catalog (Ch 204–207) and the mathematical foundations (Ch 226–232).

**Chapter 233 — GPU Denoising Algorithms** covers the full denoiser pipeline that sits between a noisy Monte Carlo render and the final frame. It explains SVGF (spatio-temporal variance-guided filtering): temporal accumulation with exponential moving average, moment-based variance estimation, and the À-Trous wavelet bilateral filter with luminance/normal/depth edge-stopping functions — grounded in the Q2RTX GLSL shader implementation. Adaptive SVGF (ASVGF) adds per-pixel sample count adaptation and gradient-based disocclusion detection. NRD (NVIDIA Real-time Denoisers) is covered in depth: ReBLUR (hit-distance-guided blur radius), ReLAX (YCoCg history clamping via variance), signal normalisation, and the NRD SDK Vulkan integration sequence. OIDN (Intel Open Image Denoise) is covered via its HIP/SYCL/CUDA GPU backends and VkDeviceMemory external-memory interop. The chapter closes with signal-specific strategies (soft shadows, AO, indirect diffuse/specular) and Vulkan render-graph integration patterns (ping-pong history buffers, timeline semaphore chaining).

**Chapter 234 — GPU Spectral Rendering and Colorimetric Algorithms** covers physically accurate colour computation from spectral first principles. It explains hero-wavelength path tracing (single-wavelength MIS across wavelength samples), spectral upsampling from RGB (Mallett–Yuksel smooth metamers, Jakob–Hanika bounded spectrum — verified against PBRT-v4 source), dispersion (Cauchy/Sellmeier IOR in GLSL), fluorescence (bispectral BRDF, Stokes shift), and polarised light rendering (Stokes vector + Mueller matrix propagation). Spectral sky models are covered: Preetham analytic evaluation, Hosek–Wilkie dataset lookup, and Bruneton–Neyret precomputed atmospheric scattering LUTs. The chapter closes with CIE colour science on GPU (CMF quadrature, XYZ→Lab/LCH conversion), gamut mapping algorithms (CUSP boundary descriptor, soft-knee compression in ZCAM/CAM16), and the mapping from spectral path tracer output to the Linux HDR display pipeline (wp_color_management, DRM HDR metadata).

**Chapter 235 — GPU Vector Graphics and 2D Path Rendering** covers the algorithms that rasterise curves and paths on the GPU without CPU-side tessellation. Loop–Blinn GPU cubic Bézier rendering is explained in full — implicit function sign test, triangle hull construction, fragment shader cubic classification (serpentine/cusp/loop/quadratic) — with the degenerate classification table corrected against Tables 1–2 of the original paper. Polar Stroking (Kilgard, SIGGRAPH 2020) is covered for tessellation-free GPU stroke expansion. Jump Flooding Algorithm 2D SDF generation and multi-channel SDF font rendering (msdfgen algorithm, median-of-three GLSL) are treated in full. GPU path rendering pipelines are compared: Vello/piet-gpu (Vulkan compute prefix-sum encode→coarse→fine pipeline), Pathfinder (tile-based mask+solid fast path), and the Skia Graphite Vulkan backend used in Chrome on Linux. GPU SVG rasterisation, COLRv1 paint graph evaluation, Porter-Duff compositing and CSS blend modes, and mesh/conic gradient GPU rendering complete the chapter.

### Scene Analysis Algorithm Chapters (Ch 236–238)

Three chapters cover the algorithms that extract semantic meaning, geometry, and object pose from GPU-processed sensor data — the bridge between the Linux GPU stack and computer vision and AR/XR applications.

**Chapter 236 — GPU 3D Scene Understanding and Semantic Segmentation** covers the pipeline from raw sensor input to semantic scene label. 2D segmentation is covered (DeepLabV3+ ASPP, Mask2Former masked cross-attention pixel decoder, SAM/SAM2 with ViT encoder and two-way transformer mask decoder, MobileSAM for real-time embedded Linux). 3D point cloud segmentation covers PointNet++ set abstraction (farthest point sampling, ball query, mini-PointNet MLP — with GPU CUDA kernels), RandLA-Net, and sparse convolution (MinkowskiEngine SparseTensor API, spconv, TorchSparse). Open-vocabulary 3D understanding covers Gaussian Grouping (CLIP+SAM per-Gaussian features), LangSplat (autoencoder language field in 3DGS), and LERF (relevancy score volume rendering). GPU occupancy grids, semantic OctoMap, 3D scene graph generation via GPU GNN inference, semantic NeRF/3DGS, and Linux deployment via ONNX Runtime MIGraphX EP with DMA-BUF zero-copy complete the chapter.

**Chapter 237 — GPU Depth Estimation and Dense Reconstruction** covers the full pipeline from image to 3D geometry. Monocular depth inference is treated (DPT, MiDaS, Depth Anything v2 metric/relative, ZoeDepth — ONNX Runtime ROCm/TensorRT deployment). Stereo depth algorithms are covered beyond SGM: RAFT-Stereo (4D correlation volume + GRU iterative refinement), CREStereo, HITNet, and the GPU correlation layer implementation. RGB-D processing algorithms (normal estimation, hole-filling, structured light decoding), Structure-from-Motion GPU stages (cuSIFT/PopSIFT, FAISS GPU matching, GPU RANSAC/MAGSAC++, COLMAP pipeline), and multi-view stereo GPU inference (MVSNet plane sweep, UniMVSNet) are all covered. NeRF and 3DGS reconstruction training pipelines (Instant-NGP hash encoding, 3DGS densification/pruning) and mesh extraction (SuGaR, GOF, screened Poisson, Ball-Pivoting) complete the chapter alongside large-scale online reconstruction (Voxblox, NanoVDB/OpenVDB GPU, NeuralRecon).

**Chapter 238 — GPU Object Detection and 6DoF Pose Estimation** covers the full detection-to-pose pipeline. 2D detection GPU inference covers the YOLO family (YOLOv8/v9/v10/YOLO11 anchor-free architecture, GPU NMS algorithms, ONNX Runtime ROCm/TensorRT/OpenVINO deployment) and RT-DETR (HGNetv2 + hybrid encoder + deformable cross-attention decoder). 3D LiDAR detection covers PointPillars (GPU pillar scatter kernel), CenterPoint (BEV heatmap head), and VoxelNet. Multimodal detection includes BEVFusion lift-splat-shoot GPU. 6DoF pose estimation covers keypoint-based methods (PVNet pixel-wise voting, FFB6D, GPU EPnP/P3P solvers) and direct/dense methods (DUSt3R/MASt3R ViT pointmap regression, FoundPose, DeepIM render-and-compare refinement). GPU feature matching covers SuperPoint, SuperGlue (Sinkhorn GNN), LightGlue (adaptive-depth flash attention), and LoFTR. GPU RANSAC frameworks (USAC, MAGSAC++, DEGENSAC) and pose tracking (GPU Kalman filter, FoundationPose, pose graph optimisation with sparse Cholesky from Ch226) complete the chapter.

## How the Series Interrelates

The Shader Catalog (Ch 204–207) and Geometry Catalog (Ch 208–212) are complementary references operating at different stages of the GPU pipeline. The geometry chapters describe how meshes are **constructed, deformed, indexed, and queried** before rasterisation or ray-test; the shader chapters describe what the GPU **computes for each pixel or sample** once geometry is in clip space.

In practice most workloads combine both series: a character is skinned (Ch209 §39–47), its meshlets are culled and dispatched by GPU-driven rendering (Ch204 Cat I), its surface shaded with PBR (Ch205 Cat V), and ambient occlusion computed from a BVH acceleration structure (Ch211 Cat IX combined with Ch205 Cat IV). The Algorithm Selection Guide in Ch208 is the recommended entry point when the problem category is unknown.

Cross-references to the rest of the book are dense. The Shader Catalog references Ch135 (Vulkan Ray Tracing API) for ray tracing implementations, Ch154 (GPU-Driven Rendering) for indirect draw and meshlet patterns, and Ch127 (Mesh Shaders and VRS) for variable-rate shading. The Geometry Catalog references Ch135 for BVH acceleration structure construction, Ch133 (Vulkan Compute Queues) for async compute geometry passes, and Ch25 (GPU Compute) for the broader compute API landscape. Both series presuppose the Vulkan pipeline foundation established in Ch24 and the shader compilation pipeline covered in Ch152.
