# Chapter 176a: State-of-the-Art CAD AI — Generative Models, Reverse Engineering, and Agentic Design

**Target audiences:** Graphics application developers evaluating AI-assisted CAD tooling; engineers building or integrating text-to-CAD and image-to-CAD pipelines; researchers and tooling engineers who need a representation-level map of the current AI-CAD literature rather than a single tool's documentation.

---

## Table of Contents

1. [Scope: Why CAD Generation Is a Different Problem](#1-scope-why-cad-generation-is-a-different-problem)
2. [Foundational Datasets](#2-foundational-datasets)
   - 2.1 [ABC: A Big CAD Model Dataset](#21-abc-a-big-cad-model-dataset)
   - 2.2 [Fusion 360 Gallery](#22-fusion-360-gallery)
   - 2.3 [The DeepCAD Dataset and Its Descendants](#23-the-deepcad-dataset-and-its-descendants)
3. [Command-Sequence Generative Models](#3-command-sequence-generative-models)
   - 3.1 [DeepCAD: CAD as a Language](#31-deepcad-cad-as-a-language)
   - 3.2 [SkexGen: Disentangled Codebooks](#32-skexgen-disentangled-codebooks)
   - 3.3 [SketchGen and Vitruvion: Constrained Sketches](#33-sketchgen-and-vitruvion-constrained-sketches)
4. [Cutting-Edge B-Rep Frameworks: Generation and Representation Learning](#4-cutting-edge-b-rep-frameworks-generation-and-representation-learning)
   - 4.1 [BrepGen and BrepDiff: Diffusion-Based Direct Generation](#41-brepgen-and-brepdiff-diffusion-based-direct-generation)
   - 4.2 [BRepCLIP and BRep-BERT: B-Rep Representation Learning](#42-brepclip-and-brep-bert-b-rep-representation-learning)
5. [Text-to-CAD: Two Competing Philosophies](#5-text-to-cad-two-competing-philosophies)
   - 5.1 [Generate the Command Sequence Directly](#51-generate-the-command-sequence-directly)
   - 5.2 [Generate Code in an Existing CAD Language](#52-generate-code-in-an-existing-cad-language)
6. [Leading CAD Scripting and Programming Languages](#6-leading-cad-scripting-and-programming-languages)
   - 6.1 [CadQuery and build123d: The Python/OCCT Family](#61-cadquery-and-build123d-the-pythonocct-family)
   - 6.2 [OpenSCAD and JSCad: The CSG-Script Tradition](#62-openscad-and-jscad-the-csg-script-tradition)
   - 6.3 [replicad: OCCT in the Browser](#63-replicad-occt-in-the-browser)
   - 6.3a [OpenGeometry: A Native Rust/WASM CAD Kernel](#63a-opengeometry-a-native-rustwasm-cad-kernel)
   - 6.4 [KCL: A Language Designed for LLM Generation](#64-kcl-a-language-designed-for-llm-generation)
   - 6.5 [Onshape FeatureScript: Server-Side, Parasolid-Backed](#65-onshape-featurescript-server-side-parasolid-backed)
   - 6.6 [Language Feature Comparison](#66-language-feature-comparison)
7. [Reverse Engineering: Point Clouds, Meshes, and Images to CAD](#7-reverse-engineering-point-clouds-meshes-and-images-to-cad)
   - 7.1 [ComplexGen: B-Rep as a Chain Complex](#71-complexgen-b-rep-as-a-chain-complex)
   - 7.2 [CAD-Recode: Point Clouds to Executable Code](#72-cad-recode-point-clouds-to-executable-code)
   - 7.3 [CAD-Coder: Images to Executable Code](#73-cad-coder-images-to-executable-code)
   - 7.4 [Point2Cyl and CSGNet: Differentiable and Program-Synthesis Primitive Fitting](#74-point2cyl-and-csgnet-differentiable-and-program-synthesis-primitive-fitting)
   - 7.5 [Implicit Surface Reconstruction: Point Clouds to Meshes](#75-implicit-surface-reconstruction-point-clouds-to-meshes)
   - 7.6 [Mesh and Point Cloud to NURBS/B-Rep: Primitive Segmentation and Surface Fitting](#76-mesh-and-point-cloud-to-nurbsb-rep-primitive-segmentation-and-surface-fitting)
   - 7.7 [Traditional Auto-Conversion Tools](#77-traditional-auto-conversion-tools)
8. [Multimodal and Unified Models: CAD-MLLM](#8-multimodal-and-unified-models-cad-mllm)
9. [Agentic and Tool-Verified Design](#9-agentic-and-tool-verified-design)
10. [Benchmarks and the Evaluation Problem](#10-benchmarks-and-the-evaluation-problem)
    - 10.1 [CADBench: Fragmentation Made Comparable](#101-cadbench-fragmentation-made-comparable)
    - 10.2 [MUSE: Beyond Geometric Similarity](#102-muse-beyond-geometric-similarity)
11. [Commercial Landscape](#11-commercial-landscape)
12. [Open Problems](#12-open-problems)
13. [Roadmap](#13-roadmap)
14. [Integrations](#integrations)

---

## 1. Scope: Why CAD Generation Is a Different Problem

The broader field of 3D generative AI — text-to-3D, image-to-3D — almost always targets a mesh, a point cloud, or an implicit field: a representation a renderer can consume directly, with no requirement that it correspond to anything a human designer would have built. CAD generation targets something narrower and stricter: a model that is either a valid **parametric construction history** (a sequence of sketch-and-extrude-style operations a CAD kernel can replay) or a valid **boundary representation** (watertight, manifold NURBS topology a kernel like OCCT can accept for further editing, Boolean operations, or STEP export). A mesh that merely looks like the target object is not a CAD model; a CAD-generation system that emits invalid topology has produced nothing usable, regardless of how close its silhouette is.

This validity constraint is why CAD-AI research reads differently from image- or mesh-generation research: papers spend as much space on execution/validity rates (does the generated program run at all? is the resulting solid watertight?) as they do on visual or geometric similarity to a target. It is also why the field has split along a representation axis that recurs across every task this chapter covers — generate a **command sequence** in a fixed CAD-operation vocabulary, generate **code** in an existing scripting CAD language (CadQuery, OpenSCAD-style), or generate a **B-rep** directly as geometry/topology — a split this book's coverage of Chapter 176 §9.6 already surfaces for the narrower case of OCCT-based Text-to-CAD tooling; §6 surveys the competing scripting languages the code-generation strategy can target. This chapter surveys that landscape at the representation level: what each family of approach actually outputs, what training data it depends on, and how the field currently measures whether any of it works.

## 2. Foundational Datasets

Every model in this chapter is trained or evaluated against one of a small number of shared datasets, so understanding them first clarifies what "state of the art" is actually state-of-the-art *on*.

### 2.1 ABC: A Big CAD Model Dataset

The ABC dataset assembled roughly one million real-world CAD models — collections of explicitly parametrized curves and surfaces sourced from public CAD repositories — specifically to give geometric deep learning methods ground-truth differential quantities, patch segmentation, and geometric feature labels that are otherwise expensive to produce by hand. [Source](https://arxiv.org/abs/1812.06216) It predates the generative-CAD wave by several years and functions today mainly as a geometry-processing benchmark (surface normal estimation, patch segmentation) and as raw material other datasets subsample or re-derive from, rather than as a generative-model training set in its own right.

### 2.2 Fusion 360 Gallery

Autodesk Research's Fusion 360 Gallery dataset is built from real, human-authored Fusion 360 design histories rather than static geometry, and ships as three components: a **Reconstruction** dataset of 8,625 human-designed sketch-and-extrude CAD sequences; a **Segmentation** dataset of 35,680 parts with each face labeled by the modeling operation most responsible for creating it; and tooling built on the Fusion 360 API for traversing B-rep data and converting between formats. [Source](https://arxiv.org/abs/2010.02392) [Source](https://github.com/AutodeskAILab/Fusion360GalleryDataset) Because it captures actual design *sequences* rather than final shapes, it is the dataset of choice for reconstruction and reverse-engineering methods that need to reproduce not just a shape but a plausible modeling history for it.

### 2.3 The DeepCAD Dataset and Its Descendants

DeepCAD (§3.1) introduced its own 178,238-model dataset of CAD construction sequences, derived from ABC. [Source](https://arxiv.org/abs/2105.09492) This DeepCAD dataset has since become the substrate for most text-to-CAD work: Text2CAD's annotation pipeline (§5.1) runs open-source LLMs and VLMs over the DeepCAD dataset to attach 660K text prompts across four description-complexity levels to its 170K models, rather than collecting a new geometry corpus from scratch. [Source](https://arxiv.org/abs/2409.17106) CAD-MLLM's Omni-CAD dataset (§8) similarly builds its ~450K multimodal instances on top of the same underlying CAD-sequence corpus rather than an independent one. [Source](https://arxiv.org/abs/2411.04954) The practical consequence: nearly every model in this chapter is ultimately trained on some derivative of a few thousand real Fusion 360 histories and one large ABC-derived corpus — a data-scarcity point this chapter returns to in §12.

## 3. Command-Sequence Generative Models

### 3.1 DeepCAD: CAD as a Language

DeepCAD was the first generative model to target CAD construction sequences directly rather than a discrete shape representation like voxels or point clouds, framing the problem as sequence generation over a fixed vocabulary of CAD commands — an explicit analogy to natural-language generation. [Source](https://arxiv.org/abs/2105.09492) The vocabulary is deliberately small: six command types — `Line`, `Arc`, `Circle`, `SOL` (start-of-loop), `Extrude`, and `EOS` (end-of-sequence) — each carrying quantized parameters (endpoint coordinates for a line; sweep angle and direction flag for an arc; center and radius for a circle; sketch-plane orientation, origin, scale, extrude distances, Boolean type, and direction for an extrude). [Source](https://arxiv.org/abs/2105.09492) A Transformer autoencoder learns to reconstruct and randomly sample sequences in this vocabulary, demonstrating both shape autoencoding and unconditional random shape generation. [Source](https://arxiv.org/abs/2105.09492) Because every generated sequence is, by construction, a valid sketch-and-extrude history, DeepCAD's outputs are trivially replayable in a real CAD kernel — the tradeoff is that the fixed six-command vocabulary caps expressiveness at prismatic, sketch-extrude geometry; it cannot represent a filleted edge, a lofted surface, or a swept profile.

**Command parametrization.** Each command carries a different subset of a shared 16-slot parameter vector — a design the reference implementation encodes directly as a fixed-size argument mask rather than a variable-length schema per command:

| Command | Parameters |
|---|---|
| `⟨SOL⟩` | none (marks the start of a new sketch loop) |
| `Line` | `x, y` — line endpoint |
| `Arc` | `x, y, α, f` — endpoint, sweep angle, counter-clockwise flag |
| `Circle` | `x, y, r` — center, radius |
| `Extrude` | `θ, φ, γ` (sketch-plane orientation) + `px, py, pz, s` (plane origin, sketch scale) + `e1, e2, b, u` (extrude distances, Boolean type, extent type) |
| `⟨EOS⟩` | none (marks end of sequence) |

[Source](https://arxiv.org/abs/2105.09492) The reference implementation's `cadlib/macro.py` defines this same 16-slot layout and the per-command argument mask that zeroes out the slots a given command doesn't use:

```python
# DeepCAD/cadlib/macro.py:16-22,27-32 (github.com/ChrisWu1997/DeepCAD)
# (lines 23-26, the SOL_VEC/EOS_VEC padding vectors, elided)
PAD_VAL = -1
N_ARGS_SKETCH = 5  # sketch parameters: x, y, alpha, f, r
N_ARGS_PLANE = 3   # sketch plane orientation: theta, phi, gamma
N_ARGS_TRANS = 4   # sketch plane origin + sketch bbox size: p_x, p_y, p_z, s
N_ARGS_EXT_PARAM = 4  # extrusion parameters: e1, e2, b, u
N_ARGS_EXT = N_ARGS_PLANE + N_ARGS_TRANS + N_ARGS_EXT_PARAM
N_ARGS = N_ARGS_SKETCH + N_ARGS_EXT

CMD_ARGS_MASK = np.array([[1, 1, 0, 0, 0, *[0]*N_ARGS_EXT],  # line
                          [1, 1, 1, 1, 0, *[0]*N_ARGS_EXT],  # arc
                          [1, 1, 0, 0, 1, *[0]*N_ARGS_EXT],  # circle
                          [0, 0, 0, 0, 0, *[0]*N_ARGS_EXT],  # EOS
                          [0, 0, 0, 0, 0, *[0]*N_ARGS_EXT],  # SOL
                          [*[0]*N_ARGS_SKETCH, *[1]*N_ARGS_EXT]])  # Extrude
```

Every continuous parameter — coordinates, angles, distances — is normalized into a bounded range and then uniformly quantized to 256 levels, so the entire command sequence becomes a sequence of discrete tokens a Transformer can predict with a softmax over a fixed vocabulary rather than regress as continuous values. [Source](https://arxiv.org/abs/2105.09492) Training minimizes a loss that sums a cross-entropy term over the predicted command type and a separately-weighted cross-entropy term over each command's quantized parameters:

$$\mathcal{L} = \sum_{i=1}^{N_c} \ell(\hat{t}_i, t_i) \;+\; \beta \sum_{i=1}^{N_c} \sum_{j=1}^{N_p} \ell(\hat{p}_{i,j}, p_{i,j})$$

with $\beta = 2$ weighting the parameter loss against the command-type loss, summed over $N_c$ commands and $N_p = 16$ parameter slots per command. [Source](https://arxiv.org/abs/2105.09492) A parameter is scored correct at evaluation time if it falls within a tolerance of $\eta = 3$ quantization levels (out of 256) of the ground truth — the accuracy metric later benchmark papers in §10 inherit. [Source](https://arxiv.org/abs/2105.09492)

### 3.2 SkexGen: Disentangled Codebooks

SkexGen (ICML 2022) targets the same sketch-and-extrude command-sequence space as DeepCAD but restructures the generative model around three separate Transformer-encoded **codebooks** — disentangling a CAD sequence's topology, geometry, and extrusion variation into independently sampleable latent codes rather than one entangled latent vector. [Source](https://arxiv.org/abs/2207.04632) Autoregressive Transformer decoders then generate sequences conditioned on combinations of codebook vectors, which gives a user (or a downstream search process) explicit control: hold the topology code fixed and vary geometry to get shapes with the same construction structure but different proportions, or mix codes from two different models to explore a design space between them. [Source](https://arxiv.org/abs/2207.04632) This disentanglement — separating *what kind of shape this is* from *what its specific dimensions are* — is a recurring idea in later CAD-generation work, echoing the parametric-vs-instance distinction Chapter 176 §9.1's CodeCAD discussion draws for hand-written code-CAD models.

**Codebook mechanics.** Each codebook is a discrete dictionary of learned vectors — 500 entries for topology, 1,000 each for geometry and extrusion — and an encoder output is mapped onto the codebook by nearest-neighbor lookup in Euclidean distance, the same vector-quantization step VQ-VAE popularized for discrete image tokenization. [Source](https://arxiv.org/abs/2207.04632) Because that nearest-neighbor lookup is non-differentiable, gradients are passed straight through the quantization step, and training combines three terms — sequence reconstruction, a codebook loss that pulls the dictionary toward encoder outputs, and a commitment loss weighted by $\beta = 0.25$ that pulls the encoder toward the dictionary instead of drifting arbitrarily:

$$\mathcal{L} = \sum_K \mathrm{CE}(h^{out}_K, h^{gt}_K) \;+\; \lVert \mathrm{sg}[Z^e] - b \rVert_2^2 \;+\; \beta \lVert Z^e - \mathrm{sg}[b] \rVert_2^2$$

where $\mathrm{sg}[\cdot]$ denotes a stop-gradient. [Source](https://arxiv.org/abs/2207.04632) The autoregressive decoders are trained with teacher forcing — conditioned on ground-truth prior tokens rather than their own predictions — which is standard practice for sequence Transformers but means SkexGen's reported sample quality, like DeepCAD's, is measured under exposure to ground truth at training time that is unavailable at generation time. [Source](https://arxiv.org/abs/2207.04632)

### 3.3 SketchGen and Vitruvion: Constrained Sketches

Every model in §3.1-§3.2 generates *unconstrained* curves: a DeepCAD or SkexGen line or arc is a set of coordinates, with no explicit record that two edges are meant to be parallel, tangent, or share an endpoint exactly. A real parametric sketch in Fusion 360 or SolidWorks is not just geometry — it is geometry plus a **constraint graph** (coincident points, parallel or perpendicular lines, tangency, symmetry, equal-length relations) that is what makes the sketch re-editable: drag one dimension and the constraint solver keeps every relation intact. SketchGen (NeurIPS 2021) targets this gap directly, using a Transformer to autoregressively generate both primitives and the constraints between them in one sequential language, with primitive and constraint types distinguished by tagged tokens so the model can share parameters across related entities; the generated primitive-plus-constraint graph can then be handed to a real geometric constraint solver for regularization, rather than being accepted as-is. [Source](https://arxiv.org/abs/2106.02711) Vitruvion (ICLR 2022) takes a similar two-stage autoregressive approach — sample primitives, then sample constraints conditioned on them — and additionally conditions generation on a raster image of a hand-drawn sketch, targeting autocompletion and constraint inference for an already-partially-specified design rather than only unconditional generation. [Source](https://arxiv.org/abs/2109.14124) Neither model is evaluated in this chapter's later benchmarks (§10), but the capability they add — an explicit, solver-checkable constraint graph — is precisely the property command-sequence models without constraints lack, and directly informs the editability leg of the three-way tradeoff §12 returns to.

## 4. Cutting-Edge B-Rep Frameworks: Generation and Representation Learning

The models in §3 all generate a *construction history* — a script that, when replayed, produces geometry. A separate and more recent line of work instead operates on the **B-rep itself**, either generating it directly (§4.1) or learning general-purpose embeddings of it for retrieval and evaluation (§4.2) — treating boundary representation the way image and language models treat pixels and tokens, as a native substrate for representation learning rather than an output format to be reached only via a command sequence.

### 4.1 BrepGen and BrepDiff: Diffusion-Based Direct Generation

BrepGen generates a B-rep as a diffusion process over a structured latent representation: a hierarchical tree whose root represents the whole solid and whose descendants represent each face, edge, and vertex, with geometry attached to each node as a bounding box plus a local shape latent code — a face node carries a bounding box $F_p \in \mathbb{R}^6$ and a $4{\times}4{\times}3$ latent shape grid $F_z$, an edge node carries its own bounding box plus a joint edge-vertex latent $E_{zv}$, and a vertex node carries raw $(x,y,z)$ coordinates. [Source](https://arxiv.org/abs/2401.15563) Each stage follows the standard DDPM forward process — Gaussian noise added over $T$ steps, $q(\mathbf{x}_t \mid \mathbf{x}_0) = \mathcal{N}(\mathbf{x}_t; \sqrt{\bar\alpha_t}\,\mathbf{x}_0, (1-\bar\alpha_t)\mathbf{I})$ with $\bar\alpha_t = \prod_{i=1}^t (1-\beta_i)$ under a linear noise schedule — and a network trained to predict the injected noise under an L2 regression loss, $\mathcal{L} = \mathbb{E}_{t,\mathbf{x}_0,\epsilon_t}\big[\lVert \epsilon_t - \epsilon_\theta(\sqrt{\bar\alpha_t}\mathbf{x}_0 + \sqrt{1-\bar\alpha_t}\epsilon_t,\, t) \rVert^2\big]$, exactly as in DDPM. [Source](https://arxiv.org/abs/2401.15563) What makes this a *cascade* rather than one diffusion run is the factorization the four Transformer-based denoisers are trained to respect — face position first, then face geometry conditioned on it, then edge position conditioned on the finished faces, then joint edge-vertex geometry conditioned on edge position: $p(F,E,V) = p(E_{zv} \mid E_p, F)\,p(E_p \mid F)\,p(F_z \mid F_p)\,p(F_p \mid \varnothing)$. [Source](https://arxiv.org/abs/2401.15563) At inference, the face- and edge-position denoisers run full DDPM sampling ($T=250$ to $0$) for precise bounding boxes while the two geometry denoisers use the faster PNDM sampler (200 forward passes), until the result is a watertight solid. [Source](https://arxiv.org/abs/2401.15563) The significance is in what this unlocks versus the command-sequence family: prior CAD-generation methods were limited to simple prismatic shapes (the sketch-extrude vocabulary's ceiling), while BrepGen is presented as the first CAD-generative model to produce free-form and doubly curved surfaces — geometry no fixed sketch-and-extrude command set can express — validated in part on a newly collected furniture dataset chosen to showcase non-prismatic complexity. [Source](https://arxiv.org/abs/2401.15563) The cost is that BrepGen's outputs are geometry, not an editable feature history — a diffusion-generated B-rep does not carry the "which sketch, which extrude" provenance a command-sequence model's output does, which matters for a downstream user who wants to *edit* the result rather than just consume the shape.

BrepDiff (SIGGRAPH 2025) targets the same direct-B-rep-diffusion problem but collapses BrepGen's cascade of dependent stages into a single-stage diffusion transformer, denoising a **masked UV-grid representation** — structured point samples across each face, carried with per-sample visibility masks — as its token set, rather than denoising topology and per-entity geometry as separate cascaded stages. [Source](https://brepdiff.github.io/) An asynchronous, shifted noise schedule lets the model learn a good distribution over plausible UV grids without ever explicitly encoding topology; a valid solid B-rep is instead recovered afterward by a post-processing pass that extends faces and resolves their intersections. [Source](https://brepdiff.github.io/) [Source](https://dl.acm.org/doi/10.1145/3721238.3730698) Because the UV-grid representation is explicit rather than buried inside a multi-stage latent pipeline, BrepDiff reports supporting direct geometry manipulation — shape autocompletion, merging two B-reps, and interpolating between them — operations BrepGen's cascaded, topology-first pipeline makes harder to expose. [Source](https://brepdiff.github.io/)

### 4.2 BRepCLIP and BRep-BERT: B-Rep Representation Learning

Generation is not the only task a B-rep foundation model can target: BRepCLIP and BRep-BERT instead learn general-purpose B-rep *embeddings*, useful for retrieval, classification, and evaluation rather than generation itself.

BRepCLIP tokenizes a CAD model as a sequence of face and edge tokens, drawn from separate discrete vocabularies for surface geometry (planar, cylindrical, toroidal, NURBS, ...) and curve geometry (line, arc, B-spline, ...) augmented with spatial descriptors, and aggregates these tokens with a Transformer encoder into a single global B-rep embedding aligned — via a CLIP-style contrastive objective — with the text and image embedding spaces of a pretrained CLIP model. [Source](https://arxiv.org/abs/2606.05515) Concretely, the vocabularies come from two separately-trained discrete autoencoders — a face dVAE with an 8,192-entry codebook and an edge dVAE with a 2,048-entry codebook, each with 256-dimensional latents, trained with a reconstruction-plus-regularization loss combining Chamfer distance against the sampled surface/curve and a KL term. [Source](https://arxiv.org/abs/2606.05515) The alignment objective is a symmetric InfoNCE loss over $\ell_2$-normalized embeddings with a learnable temperature $\tau$:

$$\mathcal{L}_{bt} = -\frac{1}{2N}\sum_i \left[\log\frac{\exp(Z_i^B \cdot Z_i^T / \tau)}{\sum_j \exp(Z_i^B \cdot Z_j^T / \tau)} + \log\frac{\exp(Z_i^T \cdot Z_i^B / \tau)}{\sum_j \exp(Z_i^T \cdot Z_j^B / \tau)}\right]$$

with an identically-structured $\mathcal{L}_{bi}$ for the image branch and a total objective $\mathcal{L} = \mathcal{L}_{bt} + \mathcal{L}_{bi}$ — the same batch-contrastive formulation CLIP itself uses, just with a B-rep encoder standing in for CLIP's image tower. [Source](https://arxiv.org/abs/2606.05515) On text-to-CAD retrieval the authors report BRepCLIP outperforming point-cloud-based baselines by roughly 40%, 22%, and 24% on the ABC, CADParser, and Automate benchmarks respectively, and on zero-shot classification it transfers to the unseen FabWave dataset without any fine-tuning, again ahead of point-cloud counterparts. [Source](https://arxiv.org/abs/2606.05515) The paper's second contribution, **BRepCLIP-Score**, is arguably the more consequential one for this chapter's §10: a geometry-aware similarity metric for scoring text- or image-conditioned CAD generation, defined simply as $\mathrm{BRepCLIP\text{-}Score}(t, x) = \cos\big(f_{\mathrm{text}}(t),\, f_{\mathrm{3D}}(x)\big)$ — cosine similarity in the same aligned embedding space the contrastive loss above trained — that the authors report correlates with human expert judgment more reliably than either raw CLIP score or Chamfer distance, both of which operate on rendered pixels or sampled points rather than on B-rep structure directly. [Source](https://arxiv.org/abs/2606.05515)

BRep-BERT applies the same masked-pretraining idea BERT popularized for text directly to B-rep graphs: a GNN tokenizer first assigns each B-rep entity (face, edge, vertex) a discrete token carrying its geometric and structural semantics, an entity sequence is then built from the graph's structural relationships, and the model is pretrained via Masked Entity Modeling — predicting a proportion of masked entity tokens from their surrounding context, exactly as BERT predicts masked words. [Source](https://dl.acm.org/doi/10.1145/3583780.3614795) A learnable relative-position encoding folded into the attention module addresses the attention-sparsity problem large B-rep graphs otherwise create. [Source](https://dl.acm.org/doi/10.1145/3583780.3614795) The motivation given for pretraining on this scale is the same one that motivates masked language-model pretraining generally: labeled B-rep datasets for any single downstream task are small, so a self-supervised objective on unlabeled B-rep graphs lets the model learn general structural priors before fine-tuning on whatever specific, label-scarce task it will be evaluated on. [Source](https://dl.acm.org/doi/10.1145/3583780.3614795)

Read together, §4.1 and §4.2 mark a genuine shift from the rest of this chapter: every other model surveyed here treats a CAD-command sequence as the interchange format between language and geometry. BrepDiff, BRepCLIP, and BRep-BERT treat the B-rep graph itself as that interchange format — closer in spirit to how vision and language foundation models treat pixels and tokens as a native substrate, rather than routing everything through an intermediate scripting language.

## 5. Text-to-CAD: Two Competing Philosophies

Once a base representation and dataset exist, the natural next step is conditioning generation on a user's text prompt — and the field has settled into two genuinely different strategies for doing so, which do not compete on the same axis so much as trade one constraint for another.

### 5.1 Generate the Command Sequence Directly

Text2CAD (NeurIPS 2024 Spotlight) trains an end-to-end Transformer to autoregressively generate a full DeepCAD-style construction-command sequence directly from a natural-language prompt, spanning prompts from beginner-level abstract descriptions ("generate two concentric cylinders") to expert-level fully dimensioned instructions. [Source](https://sadilkhan.github.io/text2cad-project/) [Source](https://arxiv.org/abs/2409.17106) Its main contribution is arguably not the generation model itself but the **annotation pipeline** that produced training data for it: because no large corpus of (CAD model, text description) pairs existed, the authors used open-source LLMs (Mistral) and VLMs (LLaVA-NeXT) to auto-annotate the existing DeepCAD dataset with multi-level text prompts, producing roughly 660K prompts across 170K models. [Source](https://arxiv.org/abs/2409.17106) This is the same strategy — generate a fixed-vocabulary command sequence directly — as DeepCAD and SkexGen, just now conditioned on language instead of sampled unconditionally.

Related work in this same family includes CAD-Llama, which fine-tunes an LLM to emit parametric 3D CAD construction sequences directly rather than free-text code [Source](https://arxiv.org/abs/2505.04481), and CAD-GPT, which frames CAD-sequence synthesis as a spatial-reasoning task for a multimodal LLM. [Source](https://arxiv.org/pdf/2412.19663) Every model in this sub-family shares command-sequence generation's structural guarantee (the output is always a valid, replayable sketch-extrude history) and its structural ceiling (no fillets, lofts, or sweeps beyond what the fixed command vocabulary encodes).

### 5.2 Generate Code in an Existing CAD Language

The alternative strategy, already covered for the OCCT ecosystem specifically in Chapter 176 §9.6, sidesteps the fixed-vocabulary ceiling entirely: instead of generating tokens in a bespoke CAD-command language, have the model write source code in an existing, general-purpose code-CAD language — CadQuery Python being the dominant target, out of the wider field of competing CAD-scripting languages §6 surveys in full — and execute that code against a real kernel to obtain the geometry. Text-to-CadQuery fine-tunes LLMs on 170K paired (text description, CadQuery script) examples, with output quality improving consistently as model scale increases. [Source](https://arxiv.org/abs/2505.06507) CADmium takes the same code-generation strategy but fine-tunes a code-specialized language model specifically for text-driven sequential CAD design rather than a general LLM. [Source](https://arxiv.org/html/2507.09792v3)

Concretely, this family's *output* looks like an ordinary human-written CadQuery script, chaining a fluent `Workplane` API rather than emitting a token per geometric parameter — this is the shape of program the code-generation strategy targets, though a given model (§7.2, §7.3) may be trained to emit only a restricted subset of the full fluent API rather than every operation shown below:

```python
# cadquery/examples/Ex003_Pillow_Block_With_Counterbored_Holes.py:28-41
# (github.com/CadQuery/cadquery)
result = (
    cq.Workplane("XY")
    .box(length, width, thickness)
    .faces(">Z")
    .workplane()
    .hole(center_hole_dia)
    .faces(">Z")
    .workplane()
    .rect(length - cbore_inset, width - cbore_inset, forConstruction=True)
    .vertices()
    .cboreHole(cbore_hole_diameter, cbore_diameter, cbore_depth)
    .edges("|Z")
    .fillet(2.0)
)
```

Every operation here — `.hole()`, `.cboreHole()`, `.fillet()` — has no equivalent token in DeepCAD's six-command vocabulary (§3.1); this is precisely the expressiveness the code-generation strategy buys, at the cost that a generated script this long has many more places to fail to parse or execute than a fixed-length command sequence does.

This code-generation strategy trades DeepCAD-family's guaranteed-valid-but-limited output for the full expressiveness of whatever CAD-scripting language it targets (fillets, lofts, sweeps, and every other operation the underlying kernel exposes) — at the cost of validity no longer being guaranteed by construction: generated code can fail to parse, fail to execute, or execute into a non-manifold or non-watertight solid, which is precisely the failure mode the evaluation methodologies in §10 and the tool-verification pattern in §9 exist to catch.

## 6. Leading CAD Scripting and Programming Languages

§5.2, and every code-generation model in §7.2 and §7.3, target "an existing CAD language" as a monolith, but in practice half a dozen genuinely different languages compete for that role, each pairing a distinct syntax with a distinct geometry kernel. This section surveys the languages actually targeted by the AI-CAD literature and the commercial products in §11, and states plainly which claims about them are directly sourced versus flagged as unverified.

### 6.1 CadQuery and build123d: The Python/OCCT Family

CadQuery and build123d are both pure-Python libraries built on the same underlying kernel binding, OCP — described in its own README as "a Python wrapper for OCCT generated using pywrap," built primarily to serve CadQuery rather than as a fork of the older, separately-maintained pythonocc-core project. [Source](https://github.com/CadQuery/OCP) Both libraries call directly into OCCT's `BRepPrimAPI_*` (primitives — box, cylinder, sphere), `BRepAlgoAPI_*` (Booleans — fuse, cut, common), and `BRepBuilderAPI_*` (topology construction) C++ class families through this binding — the same OCCT substrate Chapter 176 documents at the C++ level. [Source](https://github.com/CadQuery/OCP/blob/master/ocp.toml) [Source](https://cadquery.readthedocs.io/en/latest/_modules/cadquery/occ_impl/shapes.html)

CadQuery exposes this substrate through a fluent, method-chaining `Workplane` API — `Workplane("front").box(width, width, thickness).faces(">Z").hole(thickness)` — the same style §5.2's pillow-block example builds on. [Source](https://cadquery.readthedocs.io/en/latest/intro.html) build123d wraps the identical OCP/OCCT kernel but offers two distinct authoring styles: a **builder mode** using Python context managers (`with BuildPart() as part:`) that implicitly accumulates whatever geometry is created inside the block, and an **algebra mode** that overloads Python's arithmetic operators directly on `Shape`/`Part`/`Sketch`/`Curve` objects — `+` for union, `-` for subtraction, `&` for intersection, e.g. `r = Box(1, 2, 3) + Cylinder(0.2, 5)` for a union and `r = Box(1, 2, 3) - Cylinder(0.2, 5)` for a cut. [Source](https://build123d.readthedocs.io/en/latest/key_concepts_algebra.html) Algebra mode's terser, more literally geometric syntax is a plausible fit for LLM code generation specifically, but no paper surveyed in this chapter reports a direct comparison between generation quality against CadQuery's method-chaining and build123d's algebra mode — an open question, not a documented result.

### 6.2 OpenSCAD and JSCad: The CSG-Script Tradition

OpenSCAD predates both of the above by over a decade and uses its own declarative, functional description language rather than a general-purpose host language — a script is a tree of CSG primitives and Boolean operators (`cube()`, `sphere()`, `union()`, `difference()`) rather than a sequence of Python method calls. [Source](https://openscad.org) Its kernel has historically been CGAL's exact Boolean-operation implementation; the Manifold library was added as an alternative, explicitly selectable backend (via Preferences, or `openscad --backend=manifold`) and reached non-experimental status in OpenSCAD's nightly builds as of 2024-09-28, with CGAL still the default at that point. [Source](https://lists.openscad.org/empathy/thread/D6KV3ZLXHLBHSITSQ5GPUZUKHURU4ABE) Whether Manifold has since become the default backend in current releases was not independently confirmed from a primary OpenSCAD source for this chapter and should be treated as unverified rather than asserted further. JSCad (OpenJSCAD.org) applies the same CSG-script philosophy in JavaScript, built on its own CSG implementation rather than OpenSCAD's or OCCT's. [Source](https://github.com/jscad/OpenJSCAD.org)

### 6.3 replicad: OCCT in the Browser

replicad returns to the OCCT family but targets the browser directly — "the library to build browser based 3D models with code" — by wrapping opencascade.js, OCCT compiled to WebAssembly via Emscripten, so a replicad script executes the same B-rep kernel as CadQuery and build123d inside a JS runtime with no native install step. [Source](https://github.com/sgenoud/replicad) This matters for the agentic-tooling pattern in §9: a browser-hosted CAD kernel is directly embeddable in a web UI an agent can drive without a local CAD install, the same way Ch244's Blender MCP bridge and Chapter 176 §9.5's `opencascade.js`/CascadeStudio stack are.

### 6.3a OpenGeometry: A Native Rust/WASM CAD Kernel

OpenGeometry (`docs.opengeometry.io`) targets the same browser deployment niche as replicad but takes the opposite architectural path: rather than compiling the existing OCCT C++ kernel to WebAssembly, it implements its own geometry kernel natively in Rust and compiles *that* to WASM via `wasm-pack build --target web`, so there is no OCCT, CGAL, or other licensed/wrapped kernel underneath it at all. [Source](https://docs.opengeometry.io/) [Source](https://registry.npmjs.org/opengeometry) Its own documentation describes it as "a high-performance CAD kernel for the web that provides 2D and 3D geometric primitives and CAD operations via WebAssembly," and the npm package description is more specific about its intended niche: "CAD kernel for the web built with Rust, WebAssembly, and Three.js for browser CAD, AEC/BIM, and geometry-heavy tools." [Source](https://docs.opengeometry.io/) [Source](https://registry.npmjs.org/opengeometry)

The kernel exposes 2D primitives (Line, Arc, Rectangle, Polyline, Curve) and 3D primitives (Polygon, Cylinder, Cuboid, Sphere, Wedge), operated on via Extrude, Offset, Sweep (profile-along-path with automatic frame calculation), Triangulate, and kernel-backed Boolean operations ("combine or cut solids"), all built over the project's own documented Boundary Representation (BREP) data structure rather than OCCT's or CGAL's. [Source](https://docs.opengeometry.io/llms.txt) Rendering is handled by a separate JavaScript/TypeScript layer, built with Rollup and distinct from the Rust/WASM core build step, that declares `three` (Three.js) as a peer dependency (`>=0.168.0`) — the Rust kernel triangulates geometry, and the JS layer hands that triangulation to Three.js for display, the same general split replicad uses for its own rendering-agnostic core. [Source](https://registry.npmjs.org/opengeometry)

Export support — STL (binary), STEP (Part 21 text), IFC (IFC4 text), and PDF (2D projections) — is explicitly marked experimental in the documentation, which is worth stating plainly rather than glossing over: this is a materially less mature export story than replicad's `blobSTEP()`/`blobSTL()`, let alone CadQuery's or build123d's OCCT-backed exporters. [Source](https://docs.opengeometry.io/llms.txt) No 2D sketch constraint solver and no multi-part assembly/mates model were found documented anywhere in the project's docs or API reference — treated here as an open question per this chapter's stated convention (§6.6), not as a claim that neither exists.

OpenGeometry is also the substrate for a separate, purpose-built architectural toolkit, **OpenPlans**, for floor-plan and building modeling (including parametric door/window "Opening" elements) — the project's own build tooling copies OpenGeometry's compiled kernel output directly into an adjacent `openplans/kernel/` directory in the same repository, confirming OpenPlans consumes OpenGeometry's kernel rather than maintaining a separate one. [Source](https://docs.opengeometry.io/) [Source](https://registry.npmjs.org/opengeometry) The project is young relative to the rest of this section — its GitHub repository was created 2024-08-22 and the npm package's first published version (`opengeometry@0.0.1`) shipped 2025-05-02 — but under active development (471 GitHub stars and commits as recent as 2026-08-28 at the time of writing), and MPL-2.0 licensed for both the kernel and the npm package (an early `0.0.1` release briefly listed ISC in its own `package.json`, since corrected). [Source](https://github.com/OpenGeometry-io/OpenGeometry) [Source](https://registry.npmjs.org/opengeometry) Like replicad, its relevance to the AI-CAD tooling in §9 is architectural rather than documented-as-a-target: a browser-native, install-free kernel is directly embeddable in a web UI an agent can drive, though — unlike CadQuery, KCL, or FeatureScript — no paper or product surveyed in this chapter names OpenGeometry as a generation target.

### 6.4 KCL: A Language Designed for LLM Generation

Zoo's KCL departs from every language above on two axes at once: it targets a proprietary, GPU-native geometry engine rather than OCCT or CGAL/Manifold — Zoo describes it as "a custom GPU-native geometry engine" built on OpenNURBS and Vulkan rather than a licensed kernel — and its authors state explicitly that "LLM-friendliness" is a design goal of the language itself, not a property later discovered in an existing language: "By representing geometry as text, KCL can reuse the rapidly-growing ecosystem of machine text tools – LLMs, summarizers, AI coding assistants... LLMs can generate KCL from a human prompt." [Source](https://zoo.dev/research/introducing-kcl) [Source](https://zoo.dev/blog/zoo-design-studio-v1) Syntactically, KCL scripts read as a pipe-chain of sketch-and-extrude operations, broadly analogous in spirit to CadQuery's `Workplane` chain — e.g. `startSketchOn('XY') |> startProfileAt([-12, 12], %) |> line([24, 0], %) |> close(%) |> extrude(10, %)`. [Source](https://github.com/KittyCAD/kcl-samples) KCL is the language behind Zoo's commercial Text-to-CAD product discussed in §11.

### 6.5 Onshape FeatureScript: Server-Side, Parasolid-Backed

Onshape FeatureScript sits apart from the rest of this section on every axis: it is Onshape's own declarative language for authoring parametric *Features* — Onshape's own built-in features (Extrude, Fillet, Helix, ...) are themselves written in it — and it executes entirely server-side against Onshape's cloud geometry servers, which run the licensed Parasolid kernel rather than OCCT, CGAL/Manifold, or a proprietary engine. [Source](https://cad.onshape.com/FsDoc/) [Source](https://www.onshape.com/en/blog/how-does-onshape-really-work) FeatureScript's documentation (FsDoc) and standard-library source are publicly viewable, but the language and its execution environment remain proprietary to Onshape — there is no local, offline FeatureScript interpreter a CAD-AI pipeline could embed the way it can embed CadQuery, build123d, OpenSCAD, JSCad, or replicad. This closed-execution property plausibly explains why no paper surveyed in this chapter targets FeatureScript as a generation output, in contrast to CadQuery's dominance below.

**Why CadQuery dominates as an LLM target.** Of the languages above, CadQuery is overwhelmingly the one AI-CAD research (§5.2, §7.2, §7.3) actually generates. The Text-to-CadQuery paper states the rationale directly: CadQuery "is a pure Python package that can be executed directly without any external software dependencies," offers "a clean, high-level API that abstracts common CAD operations... into concise, readable code," and — the point that matters most for an LLM target — "Modern large models are already good at generating Python code... in many cases they are capable of writing CadQuery directly." [Source](https://arxiv.org/abs/2505.06507) The same paper contrasts this favorably against FreeCAD (GUI-bound, not natively scriptable as a standalone Python target) and OpenSCAD (its own DSL, so a model's Python-code training prior does not transfer). [Source](https://arxiv.org/abs/2505.06507) That advantage is now attracting tooling built specifically around AI-generated CadQuery: `argus-diff`, a community project built on OCP, adds geometric diff, minimum-wall-thickness/DFM checks, and rendered previews to review AI-generated CadQuery pull requests. [Source](https://github.com/CadQuery/cadquery/discussions/2045) No source surveyed for this chapter states an explicit technical rationale (e.g., action-space size per token, licensing cost) for CadQuery's dominance over build123d specifically — both share the same OCP/OCCT substrate, and the field's preference for CadQuery over build123d as a generation target reads as a training-data and community-maturity effect rather than a documented architectural one.

### 6.6 Language Feature Comparison

| | CadQuery | build123d | OpenSCAD | JSCad | replicad | OpenGeometry | KCL | FeatureScript |
|---|---|---|---|---|---|---|---|---|
| **Host language** | Python | Python | Own declarative DSL | JavaScript | JavaScript/TypeScript | Own kernel (Rust, compiled to WASM), driven from JavaScript/TypeScript | Own DSL ("KCL") | Own DSL, runs inside Onshape |
| **Geometry kernel** | OCCT (via OCP) | OCCT (via OCP) | CGAL (Manifold optional) | Own CSG engine | OCCT (via opencascade.js/WASM) | Own native BREP kernel (Rust) — no OCCT/CGAL dependency | Proprietary GPU-native (OpenNURBS + Vulkan) | Parasolid |
| **Kernel license** | LGPL-2.1 + OCCT exception | LGPL-2.1 + OCCT exception | GPLv3+/LGPLv3+ dual, or commercial | Same as tool license (below) | LGPL-2.1 + OCCT exception upstream, though replicad's own WASM build package (`replicad-opencascadejs`) declares MIT in its `package.json` — an unresolved discrepancy, noted but not adjudicated here | MPL-2.0 | Proprietary — built on the open-source OpenNURBS library, but Zoo's own engine on top of it is closed, unpublished, and reachable only as a hosted service | Proprietary — Parasolid + D-Cubed constraint solver, licensed by Onshape from Siemens PLM |
| **Tool's own license** | Apache-2.0 | Apache-2.0 | GPLv2 + CGAL-linking exception | MIT | MIT | MPL-2.0 (same package — no separate kernel/tool split; an early `0.0.1` release briefly listed ISC, since corrected) | MIT (app/language layer only — not the geometry engine) | Split: standard library is source-visible inside Onshape (Onshape's own wording is "open-source", but no license name or text is published anywhere); the language/interpreter itself is proprietary with no public implementation |
| **Execution environment** | Native, local | Native, local | Native desktop app | Browser or Node.js | Browser/WASM-first; a Node.js CLI (`replicad-cli`) was added later (first npm publish 2026-03-24) | Browser/WASM-first (npm package `opengeometry`, `three` declared as a peer dependency ≥0.168.0) | Local app UI, but geometry evaluation is a video stream from a hosted GPU engine | Server-side only, inside Onshape's cloud |
| **API paradigm** | Fluent method-chaining, plus an algebraic/operator-overloading free-function API (added 2.5.0, non-experimental since 2.8.0) | Builder/context-manager **or** algebra (operator overloading) | Declarative DSL | Functional/pipe composition | Fluent method-chaining | Not confirmed from sources reviewed — primitive/operation classes and functions per the API Reference, exact chaining style not verified | Pipe-chain / declarative | Declarative parameter block + imperative operation body, annotation-driven UI generation |
| **Boolean union syntax** | `a.union(b)` | `a + b` (algebra mode) | `union() { a(); b(); }` | `union(a, b)` | `a.fuse(b)` | Not confirmed — docs describe "kernel-backed boolean operations" without a verbatim code example located | `startSketchOn(...) \|> ... \|> extrude(...)`-style pipe chains | `opBoolean(context, id + "boolean1", { "tools" : qUnion([body1, body2]), "operationType" : BooleanOperationType.UNION })` — constructed from the documented signature/keys; no verbatim union example exists in FsDoc |
| **2D sketch constraint solver** | Yes — experimental `Sketch().constrain(...).solve()` | No — explicit/sequential sketches; uses a separate Joint system for 3D part positioning | No | No | No | No — not documented | Yes, added 2026 — `ezpz`, a standalone Rust solver (parallel, perpendicular, coincident, tangent, dimensional constraints) exposed in KCL via `sketch on XY { ... }`; before that, KCL sketches were explicit/sequential only | Yes, but at the Onshape application level, not authored via FeatureScript code itself |
| **Parametric feature tree vs. script-replay** | Script-replay | Script-replay | Script-replay | Script-replay | Script-replay | Not confirmed; imperative primitive/operation API suggests script-replay, analogous to the other JS/WASM libraries above, but not explicitly documented | Script-replay — the feature tree is a rendered view over the KCL program (stdlib calls carry a `feature_tree` annotation), not an independent parametric graph | History-based, regenerating feature tree (Onshape platform property) |
| **STEP export** | Yes (AP schema unspecified in docs) | Yes | No (unimplemented; tracked as an open feature request) | No | Yes — `blobSTEP()` (not `exportSTEP`), explicitly set to AP242DIS via OCCT's `write.step.schema` static | Yes — experimental, Part 21 text | Yes (application-protocol subset not specified) | N/A at language level — but the Onshape platform exports STEP AP203/AP214/AP242 |
| **STL export** | Yes | Yes | Yes | Yes | Yes — `blobSTL()` (not `exportSTL`; ASCII or binary via a `binary` flag), plus a `MeshShape.blobSTL()` for the Manifold mesh path | Yes — experimental, binary | Yes (also glTF, OBJ, PLY, FBX) | N/A at language level — but the Onshape platform exports STL with configurable resolution |
| **Bundled GUI/viewer** | No standalone app (CQ-editor is a separate repo), but `cadquery.vis.show()`/`cadquery.fig` ship an in-process trame/VTK viewer as hard dependencies | No — library only | Yes — full desktop app | Yes — hosted web viewer (also usable as a library) | No — library only | No — library only (renders via the consuming app's own Three.js scene) | Yes — Zoo Design Studio app | Yes — runs inside Onshape's own web viewer |
| **Fully offline-capable** | Yes | Yes | Yes | Yes (via local Node/CLI; hosted viewer needs network) | Yes, once loaded | Yes, once loaded | No — geometry evaluation needs the hosted engine | No — server-side only (Onshape staff confirmed no offline mode, 2020) |
| **Runs natively in-browser (no server round-trip)** | No (a third-party Pyodide/OCP.wasm build exists for build123d; the author states this was not extended to CadQuery) | No | No (native desktop is the primary build) | Yes — plain JS | Yes — OCCT compiled to WASM | Yes — Rust kernel compiled to WASM via `wasm-pack` | Partially — the KCL language/LSP runs client-side via WASM, but geometry evaluation is always a round-trip to Zoo's cloud engine | No — rendering is client-side WebGL/OpenGL, but geometry computation is server-side |
| **Install method** | `pip install cadquery` | `pip install build123d` | Desktop installer / OS package manager | `npm install @jscad/modeling` | `npm install replicad` | `npm install opengeometry` | Desktop app download or browser app; `brew install kittycad` for the CLI. No package-manager install for the language itself | N/A — authored directly in Onshape's browser IDE |
| **First public release** | 2013 by continuous package lineage (PyPI `cadquery` 0.1.0, 2013-04-24; same project throughout, not a separate package); the OCCT-direct-binding rewrite (dropping FreeCAD's Python API layer, which itself already wrapped OCCT) was tagged 2.0 on 2020-04-24 | ~2023 | ~2010 (earliest recoverable tag; project history may predate this) | ~2013 (current org repo; an earlier, differently-authored predecessor existed circa 2012 but is not independently dated) | 2021 (first npm publish `replicad@0.5.0` 2021-11-01; earliest commit 2021-06-01) | GitHub repo created 2024-08-22; first npm publish `opengeometry@0.0.1` 2025-05-02 | 2023 (`modeling-app` v0.0.3, 2023-08-14); the KCL language itself was formally introduced 2025-05-20 | Not independently confirmed for FeatureScript itself; the Onshape platform was publicly shipping updates by 2015-03-16 |
| **Governance/maintainer model** | Open-source project, "CadQuery Development Team" | Individual-maintainer-led open source | Community open source | Community open source, "JSCAD Organization" | Company-backed (QuaroTech Sàrl, per LICENSE) | Individual-maintainer-led open source (single credited npm maintainer); 471 GitHub stars as of 2026-09 | VC-backed company (Zoo, formerly KittyCAD) | Single-vendor proprietary (Onshape, wholly owned by PTC since acquisition completed 2019-11-01) |
| **Multi-part assembly support** | Yes — `Assembly` class with 8 constraint types (Point, Axis, Plane, PointInPlane, PointOnLine, FixedPoint, FixedRotation, FixedAxis) | Yes — Joint system (`RigidJoint`, `RevoluteJoint`, `LinearJoint`, `CylindricalJoint`, `BallJoint`) | No dedicated model — only informal composition via nested modules/`import()` | No dedicated model — only informal composition in code | Yes, but export-grouping only — `createAssembly()` groups named/colored parts into a structured STEP export (OCCT XCAF); no mates/joints, parts positioned by plain transforms | Not documented | Yes, shipped in Design Studio v1; mate-based "Constraints 2.0 & Assembly Mates" remained on the roadmap as of the cited source | Yes, at the Onshape platform level |
| **"LLM-friendliness" as a stated design goal** | Not stated by the maintainers (third-party AI-CAD papers describe it as LLM-friendly, but this is not a project claim) | Not stated in materials reviewed | Not stated in materials reviewed | Not stated in materials reviewed | Not stated in materials reviewed | Not stated in materials reviewed | Yes, explicitly (quoted in §6.4) | Not a founding design goal (silent in FsDoc), but actively marketed as an LLM-generation target since 2026 via the FeatureScript MCP Server |
| **Documented AI-CAD generation target** | Yes — the dominant target (§5.2, Text-to-CadQuery, plus CAD-Coder, cadrille, and others) | Not documented as a primary target in sources surveyed | Named by the Text-to-CadQuery paper as a rejected alternative (own DSL breaks the Python-code training prior) | Not documented as a target in sources surveyed | Not documented as a target in sources surveyed, though architecturally suited to the agentic pattern in §9 | Not documented as a target in sources surveyed, though architecturally suited to the agentic pattern in §9 like replicad | Yes — Zoo's own commercial Text-to-CAD/Zookeeper product (no academic paper targeting KCL specifically was located) | Yes — Onshape's own FeatureScript MCP Server (2026-08-11) enables agentic text-to-code-to-CAD workflows; also an academic target (CADFS dataset, 450k models/15 operations, CVPR 2026) |

Sourcing notes for rows not already cited in §6.1-§6.5: kernel and tool licenses are drawn from each project's own repository — OCCT's LGPL-2.1-plus-exception [Source](https://github.com/Open-Cascade-SAS/OCCT), CGAL's LGPLv3+/GPLv3+ dual license [Source](https://www.cgal.org/license.html), and the CadQuery [Source](https://github.com/CadQuery/cadquery/blob/main/LICENSE), build123d [Source](https://github.com/gumyr/build123d/blob/main/LICENSE), OpenSCAD [Source](https://github.com/openscad/openscad/blob/master/COPYING), JSCad [Source](https://github.com/jscad/OpenJSCAD.org/blob/master/LICENSE), replicad [Source](https://github.com/sgenoud/replicad/blob/master/LICENSE), and KCL/modeling-app [Source](https://github.com/KittyCAD/modeling-app/blob/main/LICENSE) LICENSE files. OpenSCAD's lack of STEP export is tracked as an open feature request rather than an oversight. [Source](https://github.com/openscad/openscad/wiki/Project:-Add-support-for-exporting-models-in-STEP-format) CadQuery's Assembly class and constraint types are documented in its own reference. [Source](https://cadquery.readthedocs.io/en/latest/assy.html) KCL's hosted-geometry-engine statement comes directly from the `modeling-app` README ("the 3D view in Design Studio is just a video stream from our hosted geometry engine... sends new modeling commands to the engine via WebSockets") [Source](https://github.com/KittyCAD/modeling-app/blob/main/README.md) and is corroborated by Zoo's own FAQ ("Zoo Design Studio requires an internet connection because processing and our geometry engine run in the cloud") [Source](https://docs.zoo.dev/docs/faq). KCL's 2D constraint solver, added in 2026, is `ezpz`, a standalone open-source Rust solver exposed in the KCL standard library. [Source](https://github.com/KittyCAD/ezpz) [Source](https://zoo.dev/blog/announcing-solver) KCL's assembly and Design Studio v1 details, including the still-roadmapped "Constraints 2.0 & Assembly Mates," are documented in Zoo's own product post. [Source](https://zoo.dev/blog/zoo-design-studio-v1) KCL's STEP/STL/glTF/OBJ/PLY/FBX export formats are listed in Zoo's FAQ. [Source](https://docs.zoo.dev/docs/faq) `modeling-app`'s first tagged release (v0.0.3, 2023-08-14) is on its GitHub releases page. [Source](https://github.com/KittyCAD/modeling-app/releases) Zoo's Text-to-CAD/Zookeeper product is documented on its own product page. [Source](https://zoo.dev/text-to-cad) OpenGeometry's licensing, kernel/rendering split, export-format maturity, and GitHub/npm release history are sourced from its own documentation site, its `llms.txt` API index, its npm registry entry, and its GitHub repository, as cited in §6.3a. [Source](https://docs.opengeometry.io/) [Source](https://docs.opengeometry.io/llms.txt) [Source](https://registry.npmjs.org/opengeometry) [Source](https://github.com/OpenGeometry-io/OpenGeometry)

CadQuery's package lineage is continuous from its first PyPI release (0.1.0, 2013-04-24) through the OCP/OCCT-direct-binding rewrite tagged 2.0 (2020-04-24), per its own changelog. [Source](https://github.com/CadQuery/cadquery/blob/master/changes.md) Its `Assembly` class exposes eight constraint types (Point, Axis, Plane, PointInPlane, PointOnLine, FixedPoint, FixedRotation, FixedAxis). [Source](https://cadquery.readthedocs.io/en/latest/assy.html) CadQuery ships an in-process 3D viewer (`cadquery.vis.show()`, `cadquery.fig`) built on trame/VTK, which are hard install dependencies, distinct from the separately maintained CQ-editor desktop app. [Source](https://github.com/CadQuery/cadquery) Its algebraic/operator-overloading free-function API, added in 2.5.0 and declared non-experimental in 2.8.0, is documented alongside the classic fluent API. [Source](https://cadquery.readthedocs.io/en/latest/free-func.html) A third-party Pyodide/OCP.wasm build enables build123d in-browser; its author states this was not extended to CadQuery. [Source](https://github.com/CadQuery/cadquery/discussions/1876)

replicad's `blobSTEP()`/`blobSTL()` methods (not `exportSTEP`/`exportSTL`) are defined in its `Shape` class, with STEP explicitly written as AP242DIS via OCCT's `write.step.schema` static. [Source](https://github.com/sgenoud/replicad/blob/main/packages/replicad/src/shapes.ts) Its `createAssembly()` function groups named/colored parts into a structured STEP export via an OCCT XCAF document, without a mates/constraint solver. [Source](https://github.com/sgenoud/replicad/blob/main/packages/replicad/src/export/assemblyExporter.ts) replicad's own WASM build package declares an MIT license despite building on LGPL-2.1-licensed OCCT, a discrepancy noted but not resolved here. [Source](https://github.com/sgenoud/replicad/blob/main/packages/replicad-opencascadejs/package.json) Its first npm publish (`replicad@0.5.0`) and earliest commit are both independently dated on GitHub/npm. [Source](https://github.com/sgenoud/replicad/commit/cc481474620930fcbccfa8600751d4b382b6c544) [Source](https://registry.npmjs.org/replicad)

FeatureScript's kernel and constraint-solver licensing ("Parasolid and constraint solver D-Cubed... which we license from Siemens PLM") and its server-side execution model are described in Onshape's own architecture blog post. [Source](https://www.onshape.com/en/blog/how-does-onshape-really-work) Its standard library's source-visible-but-unlicensed status is Onshape's own wording. [Source](https://cad.onshape.com/FsDoc/) Its `opBoolean` signature and map keys are documented in the FeatureScript standard library reference; no verbatim union example exists there, only a subtraction example. [Source](https://cad.onshape.com/FsDoc/library.html) [Source](https://cad.onshape.com/FsDoc/tutorials/create-a-slot-feature.html) STEP (AP203/AP214/AP242) and STL export are documented Onshape platform features. [Source](https://cad.onshape.com/help/Content/File/supported_file_formats.htm) The absence of an offline mode was confirmed by an Onshape moderator on the Onshape forum in 2020. [Source](https://forum.onshape.com/discussion/12650/internet-requirements) The Onshape platform was shipping updates by 2015-03-16, though no primary source dates FeatureScript's own introduction specifically. [Source](https://www.onshape.com/en/resource-center/what-is-new/whats-new-in-onshape-march-16-2015) PTC's acquisition of Onshape closed 2019-11-01. [Source](https://investor.ptc.com/resources/news/news-details/2019/PTC-Completes-Acquisition-of-Onshape/default.aspx) The FeatureScript MCP Server, enabling agentic text-to-code-to-CAD workflows, is documented in an Onshape Labs blog post dated 2026-08-11. [Source](https://www.onshape.com/en/blog/featurescript-mcp-server-enables-text-code-cad) A related Onshape/PTC post on "LLM-powered FeatureScript development" is dated 2026-03-19. [Source](https://www.onshape.com/en/blog/ai-artificial-intelligence-cloud-native-cad-pdm-platform) The CADFS dataset (450k CAD models, 15 modeling operations, FeatureScript-based representation) is described in its arXiv preprint. [Source](https://arxiv.org/abs/2605.01925)

Cells still marked "not confirmed" reflect an absence of a locatable primary source at the time of writing, not a claim that the feature does not exist — treat them as open questions for a future revision rather than as negative results.

## 7. Reverse Engineering: Point Clouds, Meshes, and Images to CAD

A distinct but related task takes an *existing* 3D observation — a scanned point cloud, a mesh, a photograph — and reconstructs a parametric or B-rep CAD model that reproduces it, rather than generating a novel shape from a text description.

### 7.1 ComplexGen: B-Rep as a Chain Complex

ComplexGen reframes B-rep reconstruction from a point cloud as detecting geometric primitives of three different orders — vertices, edges, and surface patches — together with the correspondence relationships between them, modeling the whole structure holistically as a **chain complex** rather than reconstructing each primitive type independently. [Source](https://arxiv.org/abs/2205.14573) Formally, a B-rep is a graded structure $\mathcal{C} = (V, E, F, \partial, \mathcal{P})$ — 0th-order vertices $V$, 1st-order curved edges $E$, 2nd-order curved faces $F$, geometric primitives $\mathcal{P}$ attached to each, and boundary operators $\partial_2 : F \to E$ (each face's bounding edges) and $\partial_1 : E \to V$ (each edge's endpoint vertices), forming the chain

$$F \xrightarrow{\partial_2} E \xrightarrow{\partial_1} V$$

subject to $\partial_1 \circ \partial_2 = 0$ — the algebraic-topology condition that a face's edges must close into loops, since applying $\partial_2$ then $\partial_1$ to a face must return to where it started rather than land on a dangling vertex. [Source](https://arxiv.org/abs/2205.14573) Validity is checked with counting constraints derived from this structure: every edge borders exactly two faces, every open (closed) edge has exactly two (zero) endpoints, and the face/edge/vertex incidence counts satisfy $\mathbf{FE} \times \mathbf{EV} = 2\,\mathbf{FV}$. [Source](https://arxiv.org/abs/2205.14573) The pipeline is two-stage: a sparse-CNN point-cloud encoder paired with a tri-path Transformer decoder first predicts primitives and their probabilistic mutual relationships, and then a global optimization pass recovers a definite, structurally valid B-rep chain complex by maximizing likelihood under these validity constraints and applying geometric refinement. [Source](https://arxiv.org/abs/2205.14573) The chain-complex framing is the paper's central claim: modeling vertex/edge/face relationships jointly, rather than per-primitive, produces more complete and topologically consistent reconstructions than prior per-primitive-independent methods.

### 7.2 CAD-Recode: Point Clouds to Executable Code

CAD-Recode takes the code-generation philosophy from §5.2 and applies it to reverse engineering: rather than predicting geometric primitives directly, it translates a point cloud into Python code that, when executed, reconstructs the CAD model — combining a relatively small pretrained LLM decoder with a lightweight point-cloud projector that feeds point-cloud features into the LLM's input space. [Source](https://arxiv.org/abs/2412.14042) The authors report the approach significantly outperforms prior point-cloud-to-CAD methods across the DeepCAD, Fusion360, and a real-world CC3D dataset. [Source](https://arxiv.org/abs/2412.14042) The architectural choice — small LLM plus point-cloud projector, rather than a purpose-built point-cloud-to-sequence network — mirrors vision-language model design generally, and reflects the same code-target strategy CAD-Coder (below) applies to images instead of point clouds.

### 7.3 CAD-Coder: Images to Executable Code

CAD-Coder is an open-source vision-language model fine-tuned specifically to generate editable CadQuery Python code directly from an image, rather than from a point cloud or text prompt. [Source](https://arxiv.org/abs/2505.14646) It was trained on GenCAD-Code, a purpose-built dataset of over 163K paired CAD-model images and CadQuery code, and is reported to outperform general-purpose VLM baselines (including GPT-4.5 and Qwen2.5-VL-72B) on syntax validity and 3D solid similarity, reaching a 100% valid-syntax rate on its evaluation set. [Source](https://arxiv.org/abs/2505.14646) The authors also report signs of generalization beyond the training distribution: the model successfully generates CAD code from real-world photographs and reproduces CAD operations not seen during fine-tuning. [Source](https://arxiv.org/abs/2505.14646) Img2CADSeq extends the image-conditioning idea to sequence-based diffusion rather than direct code generation, another point on the same representation axis this chapter keeps returning to. [Source](https://arxiv.org/pdf/2605.13293)

### 7.4 Point2Cyl and CSGNet: Differentiable and Program-Synthesis Primitive Fitting

§7.1-§7.3 cover two reverse-engineering strategies — holistic chain-complex detection and LLM code generation — but a third, older family fits an explicit *program* to the input geometry without training an LLM at all. Point2Cyl (CVPR 2022) targets CAD's extrusion-cylinder structure directly: a point cloud is first segmented per-point into base/barrel labels and normals with a supervised network, and the extrusion parameters (axis, sketch profile, distance) are then recovered from that segmentation via closed-form, differentiable formulas rather than a second learned decoder — so the geometric estimation step is exact given a correct segmentation, not merely learned to approximate it. [Source](https://arxiv.org/abs/2112.09329) CSGNet is the older program-synthesis end of this same family: a CNN encoder and RNN decoder parse a 2D or 3D shape top-down into a Constructive Solid Geometry program — a tree of Boolean operations over primitives — and, notably, can be trained via policy-gradient reinforcement learning (REINFORCE) directly against a rendering-similarity reward when no ground-truth program annotations exist at all, since a CSG program's discrete structure is not differentiable end-to-end. [Source](https://arxiv.org/abs/1712.08290) Both stand apart from §7.2-§7.3's LLM-code and §7.1's chain-complex strategies in the same way: correctness comes from geometric or combinatorial search grounded in the input, not from a language model's learned prior over what CAD code usually looks like — a useful check on the code-generation family's failure mode of producing code that is syntactically fluent but geometrically wrong.

### 7.5 Implicit Surface Reconstruction: Point Clouds to Meshes

§7.1–§7.4 all assume the input is already a point cloud or mesh and go straight to CAD/B-rep. But scanned or neural-rendering-derived point clouds (LiDAR, photogrammetry, or a 3D Gaussian Splat's mean positions, §21) are frequently noisy, non-uniformly sampled, and unstructured — a separate, older line of work reconstructs a clean watertight *mesh* from such a point cloud first, upstream of any CAD-specific reasoning. This step is not CAD-aware at all — it makes no attempt to recover planes, cylinders, or sketch-extrude history — but it is the de facto first stage most reverse-engineering pipelines run before segmentation and fitting (§7.6).

The classical approach, **Poisson Surface Reconstruction**, casts the problem as solving a Poisson equation: it builds an adaptive octree over the point cloud, treats the (assumed available) per-point normals as samples of an indicator function's gradient, and solves for the indicator function whose gradient best matches them, extracting the final surface as an isosurface of the result. [Source](https://hhoppe.com/poissonrecon.pdf) **Screened Poisson Surface Reconstruction** adds a data term that forces the solution to actually interpolate the input points (not just their normals) while keeping the same sparse, multigrid-solvable formulation, and remains the most widely deployed baseline — it ships as MeshLab's and CloudCompare's default point-to-mesh reconstruction filter. [Source](https://www.cs.jhu.edu/~misha/MyPapers/ToG13.pdf)

Learned methods replace the explicit Poisson solve with a neural implicit field. **Occupancy Networks** represent a shape as the decision boundary of a classifier trained to predict occupied/unoccupied for any queried 3D coordinate, extracting the final mesh via a marching-cubes variant (Multiresolution IsoSurface Extraction). [Source](https://arxiv.org/abs/1812.03828) **DeepSDF** takes the same continuous-field idea but predicts a signed distance rather than a binary label, via an *auto-decoder* — there is no encoder network; each shape's latent code is itself optimized directly by gradient descent — which supports shape completion and latent-space interpolation as a side effect of the training scheme. [Source](https://arxiv.org/abs/1901.05103) **Point2Mesh** takes a different approach entirely: it optimizes the weights of a mesh-convolution CNN, trained from scratch on nothing but the single input point cloud (a "self-prior"), to deform a coarse initial mesh until it shrink-wraps the target points — exploiting the fact that convolutional weight-sharing captures a shape's internal geometric self-similarity without any external training set. [Source](https://arxiv.org/abs/2005.11084)

Two more recent methods target the classical/learned gap directly. **Shape As Points (SAP)** makes Poisson reconstruction itself differentiable — a GPU-accelerated differentiable point-to-mesh layer — so oriented point positions and normals can be optimized end-to-end by gradient descent, either as a fast optimization-based reconstruction method on its own or as a differentiable component inside a larger learned pipeline. [Source](https://arxiv.org/abs/2106.03452) **POCO** (Point Convolution for Surface Reconstruction) avoids compressing an entire shape into one global latent vector — instead it computes a local latent feature per input point via point convolutions, then reconstructs the occupancy/SDF field at a query location by learned interpolation among nearby points' features, which measurably improves fidelity on thin structures that global-latent methods tend to over-smooth. [Source](https://arxiv.org/abs/2201.01831) **NKSR** (Neural Kernel Surface Reconstruction, NVIDIA, CVPR 2023) scales the same family to scene-level, outdoor, LiDAR-noisy point clouds by using compactly-supported learned kernel functions that keep the surface-fitting linear system sparse enough to solve with memory-efficient sparse solvers, rather than the dense or globally-coupled systems earlier methods require. [Source](https://arxiv.org/abs/2305.19590)

### 7.6 Mesh and Point Cloud to NURBS/B-Rep: Primitive Segmentation and Surface Fitting

§7.1–§7.4's chain-complex and LLM-code strategies output CAD *programs* or *topological structures*; a geometrically distinct lineage instead segments the input directly into primitive patches and analytically or differentiably *fits* each one — producing planes, cylinders, cones, and true B-spline/NURBS surface patches rather than a command sequence.

**ParSeNet** (ECCV 2020) is the reference architecture for this approach: an end-to-end trainable network jointly segments a point cloud into patches and fits each one to a parametric primitive — plane, sphere, cylinder, cone, or an **open B-spline surface patch** via a differentiable spline-fitting module — with a post-processing pass that closes small gaps between adjacent fitted patches so the result is closer to watertight. [Source](https://arxiv.org/abs/2003.12181) **HPNet** (ICCV 2021) targets the segmentation half more precisely by preserving sharp feature edges — a common ParSeNet failure mode — combining a learned semantic feature with two spectral descriptors derived from predicted geometric parameters and an adjacency matrix encoding sharp-edge structure, fused through a learned entropy-weighted combination before final mean-shift clustering. [Source](https://arxiv.org/abs/2105.10620) **NURBS-Diff** is not itself a full reconstruction pipeline but the fitting primitive several of these methods build on: a GPU/PyTorch module that computes analytic derivatives of NURBS curve/surface evaluation with respect to control points, weights, and the knot vector, so a NURBS patch can be fit to a target point cloud or image by ordinary gradient-based optimization inside a larger differentiable pipeline. [Source](https://arxiv.org/abs/2104.14547)

**Point2CAD** (CVPR 2024) is the most complete published pipeline in this lineage, chaining segmentation, fitting, and topology recovery into a single system that outputs an actual B-rep: point-cloud segmentation into primitive patches, hybrid analytic-or-neural surface fitting per patch (including an implicit neural representation for genuinely freeform surfaces that no closed-form primitive covers), then explicit edge and corner recovery from the fitted-patch intersections to assemble the final watertight B-rep. [Source](https://arxiv.org/abs/2312.04962) **SECAD-Net** (CVPR 2023) sits adjacent to this family rather than inside it: it is self-supervised (no ground-truth CAD-program labels needed at all) and outputs an editable sketch-extrude sequence — a small set of 2D profile-plus-extrusion solids combined by Boolean union — rather than fitted NURBS patches, making it closer in spirit to §7.4's Point2Cyl than to ParSeNet/Point2CAD's surface-fitting approach, despite starting from the same raw-point-cloud input. [Source](https://arxiv.org/abs/2303.10613)

A 2024 survey of the field, "Geometric Deep Learning for Computer-Aided Design," confirms this two-lineage picture is the field's own self-understanding: reverse engineering runs either through §7.1–§7.4's holistic chain-complex/LLM-code path, or through this section's older segmentation-then-fit path (point cloud → segmented primitives → fitted parametric surfaces → B-rep topology), with Point2CAD representing the current state of the art on the latter. [Source](https://arxiv.org/abs/2402.17695)

**A gap worth naming explicitly:** no published method chains 3D Gaussian Splatting directly to NURBS/B-rep. The practical pipeline today is necessarily two-stage — extract or optimize a mesh from the Gaussians first (§20's 2DGS or §21's SuGaR/GOF), then feed that mesh into §7.6's segmentation-and-fitting methods (ParSeNet, HPNet, Point2CAD) or §7.1–§7.4's point-cloud-to-CAD methods — rather than any single model trained to go from Gaussian primitives to B-rep surfaces end-to-end. Whether a splat's opacity/covariance structure carries information a fitting network could exploit directly (versus discarding it entirely at the mesh-extraction step) remains, as far as the published literature shows, an open research question rather than a solved one.

### 7.7 Traditional Auto-Conversion Tools

The learned methods above are still predominantly research code; the reverse-engineering workflow most CAD engineers actually run today is commercial or open-source tooling built on classical (non-learned) surface fitting, often with a human selecting patch boundaries interactively rather than a network segmenting them automatically. They are worth naming because they define the practical baseline the learned methods above are implicitly trying to automate away.

**Geomagic Design X** (Hexagon) is the dominant commercial scan-to-CAD tool: it offers both automatic and human-guided surfacing from mesh/point-cloud scan data, extracts feature-based parametric history (not just a dumb surface patch), and can transfer that live parametric history directly into SolidWorks, NX, Creo, or Inventor via its "LiveTransfer" feature. [Source](https://hexagon.com/products/geomagic-design-x) **PolyWorks|Modeler** (InnovMetric, also now part of Hexagon) covers the same space, extracting NURBS surfaces, curves, and parametric sketches from polygonal or point-cloud scan data. [Source](https://www.innovmetric.com/products/polyworks-modeler) **SolidWorks ScanTo3D** is the same capability built into SolidWorks itself, via a Mesh Prep Wizard for cleanup and a Surface Wizard offering both automatic and guided sub-mesh surface fitting. [Source](https://help.solidworks.com/2022/english/solidworks/scanto3d/t_simplifying_mesh_or_point_cloud_files.htm) **Fusion 360's Convert Mesh / Form environment** imports a mesh (STL/OBJ/3MF), converts it to an editable T-Spline body inside Fusion's Form environment, and "Finish Form" bakes the T-Spline into a conventional solid or surface B-rep body usable by the rest of Fusion's parametric feature tree. [Source](https://help.autodesk.com/view/fusion360/ENU/?contextId=FRM-IMPORT-TSM) T-Splines originated as an independent Rhino plugin later acquired by Autodesk, whose standalone version was subsequently discontinued in favor of this Fusion-only integration.

On the mesh-preparation side of the pipeline — before any of the above surface-fitting step runs — **Rhino's QuadRemesh** produces field-aligned quad-dominant retopology from arbitrary surfaces, meshes, or SubD input (available in Rhino directly and as Grasshopper components), explicitly marketed for reverse-engineering prep work. [Source](https://www.rhino3d.com/features/quadremesh/) **Instant Meshes** is the open-source academic equivalent — a fast field-aligned isotropic/quad-dominant remesher built around a unified smoothing operator over edge orientations and vertex positions. [Source](https://igl.ethz.ch/projects/instant-meshes/) **CloudCompare** is the open-source workhorse for the point-cloud stage specifically — registration, denoising, and segmentation of raw scan data before it reaches any of the above surfacing tools. [Source](https://www.cloudcompare.org)

**One correction worth flagging for anyone reaching for it from this book's coverage of OCCT-based tools (Ch176):** FreeCAD ships a workbench literally named "Reverse Engineering," but as of the current FreeCAD documentation it has no implemented functionality — it is a placeholder, still under development. [Source](https://github.com/FreeCAD/FreeCAD-documentation/blob/main/wiki/Reverse_Engineering_Workbench.md) The actual working NURBS curve/surface capability inside the FreeCAD ecosystem usable for this kind of work lives in the external **Curves workbench** instead. [Source](https://wiki.freecadweb.org/Curves_Workbench)

## 8. Multimodal and Unified Models: CAD-MLLM

Rather than building a separate model per input modality (text, image, point cloud), CAD-MLLM unifies all three into a single multimodal LLM that generates a CAD command sequence conditioned on any one of them, or a combination. [Source](https://arxiv.org/abs/2411.04954) It aligns the feature space of diverse input modalities with CAD command-sequence representations inside an LLM, and was trained and evaluated on a purpose-built dataset, Omni-CAD, containing roughly 450K instances — each paired with a text description, multi-view images, points, and its CAD construction sequence. [Source](https://arxiv.org/abs/2411.04954) To evaluate topology quality and surface-enclosure extent (properties generic image/mesh similarity metrics do not capture), the authors introduce dedicated metrics rather than relying solely on shape-similarity scores borrowed from mesh-generation research. [Source](https://arxiv.org/abs/2411.04954) CAD-MLLM's significance is less any single-modality result and more the demonstration that a command-sequence CAD-generation target (§3) can share one model and one training run across text, image, and point-cloud conditioning — collapsing what would otherwise be three separate research lines (§5, §7) into one architecture.

## 9. Agentic and Tool-Verified Design

Every model surveyed so far is fundamentally one-shot: prompt in, CAD output out, with no verification step in between. A separate line of work instead wraps a generation model in an agent loop that inspects its own output and iterates — the same shift Chapter 176 §9.6 documents for MCP-based tooling around CadQuery, build123d, and FreeCAD, where wrapping a generation model with tool calls that render, measure, and validate geometry measurably improves benchmark scores over blind one-shot code generation.

CADDesigner is an LLM-powered agent for *conceptual* CAD design that accepts both text descriptions and hand-drawn sketches, engages in clarifying dialogue with the user before committing to a design, and uses iterative visual feedback during model creation — rendering intermediate results and checking them before proceeding — plus a structured knowledge base of completed designs that the agent can draw on for future requests. [Source](https://arxiv.org/abs/2508.01031) "From Idea to CAD" takes a more explicitly multi-agent structure, mirroring an industrial design team: a Requirements Engineering agent handles initial specification, a CAD Engineering agent performs the actual modeling, and a Vision-Based Quality Assurance agent validates the result, iterating with user feedback in a loop. [Source](https://arxiv.org/abs/2503.04417) "Clarify Before You Draw" targets a specific, common failure mode of one-shot text-to-CAD systems directly: an underspecified or contradictory prompt has many valid interpretations, and a one-shot model must guess. Its proposed system, **ProCAD**, pairs a clarifying agent that audits the prompt and asks a targeted follow-up question only when necessary with a separate coding agent that turns the (now-disambiguated) specification into executable CadQuery code — reported to cut invalid-output rate from 4.8% to 0.9% against a Claude Sonnet baseline. [Source](https://arxiv.org/abs/2602.03045)

The common thread across all three: CAD-generation quality gates on validity and design intent in ways image generation does not, so an agent that can render its own output, measure it, or ask a clarifying question closes a feedback loop a pure one-shot generator cannot — the same lesson Chapter 176 §9.6 draws from `build123d-mcp`'s measured effect on CADGenBench scores.

## 10. Benchmarks and the Evaluation Problem

The datasets in §2 were built to *train* models; a separate and more recent wave of work exists specifically to *evaluate* them, and its own history is instructive: early evaluation was largely ad hoc geometric-similarity comparison on whatever subset of DeepCAD or Fusion360 a given paper happened to hold out, which made cross-paper comparison close to impossible. Chapter 176 §9.6 already covers two of the resulting standardized benchmarks in the OCCT/code-generation context — CADGenBench (geometric accuracy, topology correctness, and CAD validity, with a mandatory watertight/manifold check) and Text2CAD-Bench (600 human-curated parametric-CAD examples across four complexity levels) — this section covers two more general benchmarks that evaluate across representations and modalities rather than a single toolchain.

### 10.1 CADBench: Fragmentation Made Comparable

CADBench addresses exactly the fragmentation problem above directly: existing CAD-generation evaluation was scattered across different datasets, input modalities, and metrics, making direct comparison between published methods difficult even when the underlying task was the same. [Source](https://arxiv.org/abs/2605.10873) It assembles 18,000 evaluation samples spanning six benchmark families derived from DeepCAD, Fusion 360, ABC, MCB, and Objaverse, across five input modalities (clean meshes, noisy meshes, single-view renders, photorealistic renders, and multi-view renders), scored on six metrics spanning three categories:

| Category | Metric | Definition |
|---|---|---|
| Geometric fidelity | Volumetric IoU | $\lvert V(\hat S) \cap V(S) \rvert / \lvert V(\hat S) \cup V(S) \rvert$ — voxelized-occupancy overlap |
| Geometric fidelity | Chamfer Distance | $\frac{1}{\lvert \hat P \rvert}\sum \min \lVert x - y \rVert^2 + \frac{1}{\lvert P \rvert}\sum \min \lVert y - x \rVert^2$ — bidirectional point-to-surface error |
| Geometric fidelity | Surface IoU | thresholded bidirectional surface-point coverage (matched within 1% of the bounding-box diagonal) |
| Executability | Valid Shape Rate | fraction of generated programs that execute and yield a valid solid |
| Program compactness | Token Count | number of generated tokens |
| Program compactness | Operation Count | number of CAD operations in the generated program |

[Source](https://arxiv.org/abs/2605.10873) Running eleven CAD-specialized and general-purpose vision-language systems through this unified harness (over 1.4 million generated CAD programs in total) produced a finding that cuts against the code-generation optimism of §5.2 and §7.3: under idealized clean-input conditions, specialized mesh-to-CAD reconstruction models substantially outperform code-generating VLMs, which remain far from reliable CAD-program reconstruction, with quality degrading further as geometric complexity increases and model rankings shifting depending on which of the six metrics is applied. [Source](https://arxiv.org/abs/2605.10873)

### 10.2 MUSE: Beyond Geometric Similarity

MUSE argues that geometric similarity — however it is measured — is the wrong ceiling for evaluating industrial text-to-CAD, and instead evaluates three engineering-specific dimensions: whether a generated design is **manufacturable**, **functional**, and **assemblable**. [Source](https://arxiv.org/abs/2605.28579) Its evaluation protocol is staged rather than a single score, and each stage is a hard gate on the next. A **Code Check** executes the generated CadQuery script in a sandbox and confirms it constructs a solid and exports a valid STEP file — anything that fails to execute scores zero on every later stage. A **Geometric Check** then runs four binary tests against the exported STEP geometry: watertight (no open boundaries or naked edges), manifold (valid manifold topology throughout), self-intersection-free, and overlap-free (no interpenetrating solid components) — failing any one of the four blocks advancement. Only geometry that clears all four reaches **Design-Intent Alignment**, where a rubric-based VLM judge (validated against human annotation) scores six binary sub-criteria grouped into three pillars — functional and robust (functionality); well-toleranced and manufacturable (manufacturability); assembly-ready and connectable (assemblability) — with the final score the mean across the three pillars. [Source](https://arxiv.org/abs/2605.28579) The paper's central empirical finding is a **failure cascade**: models that reliably clear the Code Check fail at markedly higher rates once evaluated for valid geometry, and fail at higher rates still once evaluated against engineering-ready design specifications — meaning most of the "success rate" numbers models report against weaker benchmarks describe executability, not usable engineering output. [Source](https://arxiv.org/abs/2605.28579)

## 11. Commercial Landscape

Chapter 176 §9.6 already covers the OCCT-adjacent AI ecosystem specifically — Text-to-CadQuery-style code generation, and MCP servers (`build123d-mcp`, `cadquery-mcp-server`, FreeCAD MCP variants) that wrap existing OCCT-based scripting APIs as agent tools — plus Zoo (formerly KittyCAD) as the deliberate counterexample: a commercial Text-to-CAD product built on its own GPU-native geometry engine and a custom scripting language, KCL (§6.4), not on OCCT at all. That split — wrap an existing kernel's scripting surface with agent tooling, versus build (or train) a foundation model against a proprietary geometry engine — recurs at the largest commercial CAD vendors. Autodesk is developing an agentic assistant across Fusion and its other products, with a stated roadmap goal of generating **editable B-rep geometry** from a single prompt using Autodesk's own foundation models for manufacturing — output a user can continue editing as ordinary parametric CAD, not a static mesh. [Source](https://adsknews.autodesk.com/en/news/new-investments-in-fusion-bring-ai-powered-transformation-to-manufacturing/) The distinction Autodesk draws in its own framing — prompt-to-mesh versus prompt-to-*editable BRep* — is precisely the validity/editability boundary §1 opens this chapter with: a system that cannot hand back an editable feature history has not, by the standard this chapter's research literature applies, solved CAD generation, however good the resulting shape looks.

## 12. Open Problems

Three limitations recur across nearly every system this chapter covers, and are worth stating plainly rather than leaving implicit in each section's caveats:

**Data scarcity for real engineering parts.** Every dataset in §2 traces back to either ABC (scraped public CAD repositories, not curated for engineering realism) or a few thousand Fusion 360 hobbyist/consumer design histories. None of the datasets this chapter surveys are drawn from tolerance-critical aerospace, medical, or precision-manufacturing parts, and MUSE's failure-cascade result (§10.2) suggests the gap between "executes" and "manufacturable" is exactly where that data scarcity shows up.

**The representation ceiling is still unresolved.** Command-sequence models (§3, §5.1) guarantee valid, editable output but cap expressiveness at their fixed vocabulary; code-generation models (§5.2, §7.2, §7.3) gain full kernel expressiveness but lose the validity guarantee; diffusion-over-B-rep models (§4.1) gain free-form geometry but lose feature-history editability. No approach surveyed here has all three properties (guaranteed valid, fully expressive, and editable) simultaneously — a system architect choosing among them today is choosing which of the three to give up. Constraint-graph sketch models (§3.3) narrow this gap only at the sketch level, not the full-solid level: SketchGen and Vitruvion make a *2D sketch* re-editable via a solver-checkable constraint graph, but nothing surveyed in this chapter extends that same explicit-constraint property to a full 3D B-rep or feature history. The language choice in §6 is a related but distinct axis: even a fully expressive code-generation target (CadQuery, build123d) only gains editability in the sense that its emitted *source code* is human-readable — none of §6's languages give an LLM-generated script the same solver-checked constraint guarantees SketchGen and Vitruvion add at the sketch level.

**Evaluation is still immature relative to the pace of new models.** CADBench and MUSE (§10) are recent enough that most of the generative models in §3-§5 and §7-§9 predate them and were never evaluated against their stricter, multi-stage protocols — meaning many of this chapter's headline results were measured against benchmarks the field's own subsequent research has since argued undercount real failure modes.

**Neural-rendering-to-CAD is still a manually stitched pipeline, not an end-to-end one.** §7.6 already flags this in detail: no published method takes 3D Gaussian Splatting primitives directly to NURBS/B-rep surfaces. Today's practical route is necessarily two separate research lines composed by hand — a Gaussian-to-mesh step (Ch115 §20-21) followed by one of §7.1-§7.4's or §7.6's mesh/point-cloud-to-CAD methods — with no evidence in the literature that anyone has yet tried training a single model across that seam, or that a splat's opacity/covariance parameters would even help such a model if it existed.

## 13. Roadmap

**Near-term (6–12 months).**

- Autodesk's "Neural CAD for Geometry," announced at AU 2025, targets generating editable B-rep CAD objects from a text prompt using Autodesk's own foundation models, positioned as complementary to (not a replacement for) LLM-driven agentic workflows; no public ship date has been given. [Source](https://adsknews.autodesk.com/en/news/upcoming-3d-generative-ai-foundation-models/) [Source](https://www.research.autodesk.com/blog/ai-and-industry-transformation-at-au-2025/)
- Already shipping, ahead of the above: Autodesk's Fusion MCP server (local, tech preview) and Fusion Automation/Data MCP servers expose Fusion's own API as agent-callable tools — the same wrap-an-existing-API pattern §9 and Chapter 176 §9.6 document for `build123d-mcp`/`cadquery-mcp-server`. Revit and documentation MCP servers are listed as "coming soon," without a date. [Source](https://www.engineering.com/autodesk-announces-fusion-mcp-servers-and-more-ai-updates/)
- Community tooling is catching up to AI-generated CadQuery specifically: `argus-diff` (§6.1) adds geometric-diff and DFM review to CadQuery pull requests, a direct response to reviewing AI-generated code rather than human-written code. [Source](https://github.com/CadQuery/cadquery/discussions/2045)
- Benchmark proliferation continues past CADBench and MUSE (§10): Text2CAD-Bench and CADTests were both published within a few months of them, independently converging on stricter, multi-stage, executability-first evaluation protocols. [Source](https://arxiv.org/abs/2605.18430) [Source](https://arxiv.org/abs/2605.07807)

**Medium-term (1–3 years).**

- Zoo has publicly stated an upcoming "Conversational CAD Copilot" for KCL — image-to-CAD, automatic error fixing, and parameterization/refactoring of already-generated KCL — plus ML-assisted KCL code completion, extending Text-to-CAD from one-shot generation toward the iterative, tool-verified pattern §9 already documents for CadQuery/build123d-based agents. [Source](https://zoo.dev/blog/zoo-design-studio-v1)
- build123d's open documentation issue proposing an LLM-consumable doc export (via the `sphinx-llms-txt` extension) is a concrete, if still unresolved, signal that CAD-scripting projects are starting to treat "is this library easy for an LLM to generate correct code against" as a maintenance concern in its own right, not just a downstream research question. [Source](https://github.com/gumyr/build123d/issues/1060)
- BrepGen's own stated future work — multi-body assemblies rather than single solids only, a self-intersection loss, and learned post-processing to close watertightness gaps — maps directly onto the representation-ceiling problem §12 raises for diffusion-over-B-rep models; none of it has shipped as of the sources surveyed here. [Source](https://arxiv.org/abs/2401.15563)
- CAD-MLLM's authors identify text descriptions that under-specify precise dimensional geometry, and a vision encoder sensitive to multi-view perspective distortion, as open limitations, proposing to extract explicit CAD dimension attributes into training data as a fix — a data-representation problem, not a modeling one. [Source](https://arxiv.org/abs/2411.04954)

**Long-term.**

- MUSE's authors are explicit that its Design-Intent Alignment stage evaluates via LLM judgment against human-validated rubrics, not physical manufacture or assembly — actual manufacturability and assembly validation against real hardware is future work the paper itself calls out, not a solved problem. [Source](https://arxiv.org/abs/2605.28579)
- CADBench's authors similarly flag that operation-level program equivalence, semantic program equivalence, and correspondence to a human-authored feature tree are all still unmeasured — meaning even the newest benchmarks in this chapter (§10) do not yet check whether a generated program is the *right* program, only whether its output geometry is close enough. [Source](https://arxiv.org/abs/2605.10873)
- Newer entrants point toward smaller, more tightly-coupled model-plus-solver systems rather than ever-larger general LLMs prompted into CAD code: Arko-T, a 4B-parameter text-to-parametric-CAD model reporting competitive results against much larger frontier LLMs at a fraction of inference cost, and Embodied CAD's solver-grounded agent approach to parametric B-rep assembly modeling. This chapter has not independently verified either paper's claims beyond what each states about itself. [Source](https://arxiv.org/abs/2606.30429) [Source](https://arxiv.org/abs/2606.31252)
- Whether any model developer is explicitly re-targeting training or evaluation toward CADBench's or MUSE's specific staged protocols is, per both papers' own text, not documented anywhere surveyed for this chapter — the clearest long-term signal is simply that evaluation benchmarks keep multiplying (CADBench, MUSE, Text2CAD-Bench, CADTests, all within a few months of each other in 2026), which this chapter treats as an open trend, not a resolved convergence.

---

## Integrations

- **Ch176 (OpenCASCADE Technology):** §11.6's OCCT-specific AI-CAD coverage (Text-to-CadQuery code generation, CADGenBench, Text2CAD-Bench, and the `build123d-mcp`/`cadquery-mcp-server`/FreeCAD MCP servers) is the OCCT-toolchain-specific instance of the representation-level landscape this chapter surveys, and §6 of this chapter surveys the wider CAD-scripting-language ecosystem (CadQuery, build123d, OpenSCAD, JSCad, replicad, KCL, FeatureScript) those tools sit alongside; §9's agentic/tool-verified pattern here is the same one §11.6 documents concretely via `build123d-mcp`'s measured score improvement on CADGenBench.

- **Ch124 (Local LLM Inference on Linux):** Every model in this chapter that runs as an inference-time LLM or VLM (Text2CAD, CAD-Recode, CAD-Coder, CADDesigner, and the agentic systems in §9) is model-agnostic at the inference layer — nothing about the CAD-generation task requires a cloud-hosted model, and a locally served model along the lines Ch124 describes is a valid backend for any of the open-weights systems this chapter covers.

- **Ch244 (AI-Driven 3D Creation — Blender MCP, Claude Code, and Generative Tools):** Ch244's Blender MCP bridge and this chapter's agentic CAD systems (§9) share the same underlying pattern — wrap an existing scripting API (`bpy` vs. CadQuery/build123d/FreeCAD) behind agent-callable tools rather than training a model to emit final geometry directly — applied to mesh-based DCC tooling in Ch244's case and parametric/B-rep CAD tooling here.

- **Ch40 (Bevy and wgpu) and Ch98 (WebAssembly and WebGPU as a Deployment Target):** Any of this chapter's generated or reconstructed CAD models still needs a rendering path once produced; §11.5's `opencascade.js`/CascadeStudio browser stack and §11.2's Rust-native kernels (both in Ch176) are the visualization layers a generative or reverse-engineering pipeline from this chapter would plausibly render through.

- **Ch115 (NeRFStudio, Neural Radiance Fields, and 3D Gaussian Splatting):** §7.5's point-cloud-to-mesh reconstruction methods and §7.6's mesh/point-cloud-to-NURBS/B-rep fitting methods are the natural downstream stage for Ch115 §20-21's Gaussian-splat-to-mesh extraction (2DGS, SuGaR, Gaussian Opacity Fields) — a captured or splat-derived point cloud/mesh from Ch115 is exactly the kind of noisy, unstructured input §7.5-§7.6 assume. §7.6 also names the reverse direction explicitly as an open gap: no published method fits NURBS/B-rep surfaces to Gaussian primitives directly, without a mesh-extraction step in between.
